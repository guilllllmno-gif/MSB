# 风控原型（rc-app）· MSB 全业务风控仪表盘 + 交易监控流水线 + 关联团伙

> 给下次接手 / 新会话快速续接用。代码在 `rc-app/`，分支 `msb-lifecycle-state-transition`。
> 业务背景：加密货币型 MSB（FuturePay CA，CAD / FINTRAC 口径）。业务线：On-ramp（法币买币）、Off-ramp（卖币出金）、虚拟货币兑换（币币）、充值、提现。

## 一、技术与视觉
- React + `@heroui/react` v2 + Tailwind v4 + Vite。路由级代码分割（`React.lazy`）。
- 字体：Golos Text（全局 `--font-sans`）。**所有英文统一用 Golos Text，不使用 `font-mono`**（地址 / 哈希 / ID 也用）。
- 布局：灰色侧边栏（可收起）+ **白色内容区（无圆角包裹、无滚动条）**；深色模式用 HeroUI 原生主题。
- 圆角层级：容器 16（`.card`）/ 控件 12 / 小标签 8 / 徽章 6 / 胶囊 full。
- 详情页顶部为**扁平页头**（不用卡片框包）。共享 `components/Timeline.tsx`（HeroUI 无 Timeline，自建）。
- 配色克制：蓝（brand）+ 灰为主。**核心口径:只有「状态」可用色徽标 —— 生命周期/工作流状态(已上线/待复核/调查中/已报送…)用色;类别/类型/执行场景/风险等级/上线策略/适用对象/关联类型等「属性·取值」一律中性灰**(取值是设置值、不是状态)。避免一行里多种彩色徽标堆叠的「花花绿绿」。已落地:监控规则列表 / 规则详情(venue·风险等级·上线策略徽标转灰)、名单管理 / 名单详情(类别·类型·关联类型转灰),状态徽标保留色。表内单列的「结果/状态」单元格(处置结果·命中动作)可保留色。`Pill` 默认 `tone="grey"`。**主体/商户头像统一中性灰**(`Initials` 加 `mono` prop —— 头像不承载状态,一排彩色圆点纯属噪声):已应用于团伙列表/详情的成员头像(图谱节点 / 连线端点 / 成员卡),分析师(经手人)头像仍保留个人色作身份区分。**全站扫一遍已落地**:团伙(列表/详情/新建抽屉)、告警详情的关联团伙成员、主体档案 360(目录行 / 档案头像 / 关联主体)的商户头像 → mono 灰;`Initials` 调用方加 `mono`。**检测维度 `FDIM` 配色 → 中性灰**(事后监控列表 / 事后详情 / 案件详情的维度徽标,图标+标签仍区分维度)。**全站落地(样板=仪表盘,已确认)**:案件优先级(`PRIO_TONE`)、案件主体类型(`SUBJ_TONE`)、关联类型徽标、系统建议风险等级 → 中性灰;**保留色** = 真·状态(案件状态机 / 处置结果)+ 主体严重度焦点(案件风险分、告警 severity、团伙置信度)+ 图表 + 人物头像。事中监控 / 告警研判 / 处置记录 / 运营总览等页核对后:severity 本就以中性 chip 呈现、仅状态用色,已合规。**团伙页**进一步收敛:洗钱手法徽标(原按 risk 上色)、共享维度 chips(原 资金/地址/设备/IP 四色)、成员类型徽标 → 中性灰,**只留 置信度% + 状态 + 图谱连线维度色** 作视觉重点(图谱连线 / 置信度构成堆叠条的维度配色是分析核心,保留)。
- 提交后自动 commit + push 到 origin（见全局记忆 auto-push）。每次实质改动同步更新本文档（见记忆 update-rc-doc）。

## 二、风控仪表盘（`pages/Dashboard.tsx`，`/dashboard`，默认首页）
**MSB 全业务线风险总览**，不是单一业务看板。自上而下：
- **角色视角切换（演示态，核心）**：同一仪表盘按**当前是谁**换内容 —— 页头胶囊切 **一线分析师（James Liu）↔ 风控总管·兼MLRO（Emma Zhang）**，下方**职责分工条**（左色条 + 头像 + 角色徽标）说明该角色能做什么。两视角差异：① **需立即处理 banner** —— 分析师版为团队态势全量（超SLA / 报送 / 待认领案件 / 待认领团伙）；总管版只留「团队超SLA·需介入」（报送已并入「待我审批」、待认领已并入「团队负荷」，不重复）。② **个人队列 ↔ 团队负荷** —— 分析师显「待我处理」（个人队列）；总管显「团队负荷」（`AnalystLoad` 全员柱 + 超载人数 / 待派单计数，点柱下钻改派，见二·补）。③ **风险评分分布** —— 分析师只读（阈值由总管在全局策略调）；总管 `editable` 双滑块可拖，实时重算三区占比 + 直通率 + 较基线（40）增量（`countBelow` 跨桶按比例插值）。④ **待我审批卡** —— 总管专属（分析师不可见），见下。`role` state 切换，`ME`/`HEAD` 两身份。
- **待我审批（总管专属）**：只有风控总管能拍板的**四类决策**汇成一张审批收件箱，全部从真实 store 派生、与各自列表页同口径 —— **规则上线**（`RULES` state=pending，带回测误报 + 提交人）/ **规则变更**（已上线规则的拟议变更，`ruleStore.allPending()`，带字段 diff 摘要 + 提交人）/ **案件升级**（case state=mlro）/ **报送签发**（report `liveStatus`=review）/ **名单复核**（list status=pending），每项深链到对应详情页（规则两类落到规则详情顶部横幅审批，见六节「变更治理」）。分析师视角此卡隐藏。
- **风控流水线总览**(原 `components/PipelineMap.tsx`,曾置于仪表盘最顶)**已移除** —— 教学 / 说明类内容不在操作页占位,这张「一笔交易怎么流动」的流转地图(主链 事中→告警→案件→报送→处置;旁路汇入 事后/团伙;反馈闭环 规则回填)已收入**使用说明**(`docs/规则配置使用说明` 顶部「总览 · 风控流水线」节),组件文件随之删除。
- **需立即处理** banner（团队级）：SLA 超时 / FINTRAC 报送临期 / 冻结资金 / 待认领团伙。**系统自动推送、按时限排序，不需人工设优先级**（时钟 / 法定倒计时 / 制裁命中等客观信号驱动）。
- **业务线风控总览**（头条）：5 条业务线一张表（资金流向 / 今日笔数 / 金额 / 告警 / 直通率 / 关键风险），风险点小圆点上色。
- **风险防御 / 业务转化** 双指标面板（14 日趋势折线）—— 风控的双重价值：拦下风险 + 让合规交易放得更多更快。
- **待我处理 + 风险评分分布**（同一行）：待我处理是**个人队列**（从 `ringStore` 拉自己拥有的团伙 + 静态告警 / 报送 / 案件待办）；评分分布是 Stripe Radar 式直方图（阈值分 放行 / 人工 / 拦截 区）。
- **告警队列健康度**（运营脉搏 · 摘要卡）：整张卡可点,跳运营总览（`/ops`）。顶部**状态判定胶囊**（健康 / 偏紧 / 超负荷）+ 一行 5 格体征（待处理积压 / SLA 达成率 / MTTR / 超 SLA 件 / 团队负荷 `超载人数/总数`），数据由 `lib/opsMetrics.ts` 的 `queueHealth()` 派生 —— 与运营总览页同源,判定口径一致。完整图表（燃尽 / 停留 / 分析师负载）下沉到运营总览,仪表盘只留一眼可读的健康摘要。
- **交易处置漏斗**（业务无关）：发起 → 制裁/名单筛查 → 规则评分 → 自动放行，全业务线统一闸口。
- 收入提升机会 / Top 命中规则 / 关联团伙概览 / 高风险交易信号 / 链网络分布。
- **图表交互**：共享 `LineChart`（与 `AnalystLoad`、`DAYS14` 一并抽到 `components/charts.tsx`，供仪表盘 + 运营总览复用）自带 hover tooltip（竖向辅助线 + 各序列圆点 + 数值浮窗，用百分比定位不受 SVG 拉伸影响，支持 `labels`/`valueFmt`/`name`）；donut / 柱 / 直方图 / 网络条用原生 `<title>`；`AnalystLoad` 柱有同款浮窗。
- **可读性**：去行话、用日常词（产能→正常上限、在办→手头任务、超载→忙不过来、转化率口径标清），**每张图配一行大白话注脚**说明怎么读。
- **跨模块数据自洽（关键）** —— 仪表盘里凡是「能在别的页面点开核对」的数字都**从各 store 真实派生**，与列表页同口径，不再硬编码假数：① **需立即处理 banner** —— `告警超 SLA`（调查车道 + sla 红，同 AlertList）/ `FINTRAC 报送待处理`（review + returned，同 ReportFiling 需立即处理）/ `案件待认领`（active 且未分配，同 CaseList 显「认领」的件）/ `待认领团伙`（ringStore pending）；**零计数的 chip 自动隐去**，全清空时 banner 转绿显「已全部清空」。② **待我处理（个人队列）** —— 不再用不存在的假 ID（原 ALT-2418 / CASE-2026-014 等死链），改从**真实记录**派生：归我或未分配（待认领）的 告警（非 closed）/ active 案件 / 我起草的 active 报告 + 我拥有的团伙，**每项深链到真实详情页**（`/alert?id=` `/case?id=` `/report?id=` `/ring?id=`），超时项排前。③ **Top 命中规则** —— 取自 `lib/rules.ts` 真实 live 规则按 `hits30` 降序前 4（行可点进 `/rule?id=`），误报率 ≥20% 标红，注脚指向真实最高误报规则（新商户首充 26%）；去掉原 mock「误伤金额」列。④ 关联团伙概览（待认领/调查中/高置信 + Top3）本就 live。订阅 `useAlertVersion/useCaseVersion/useReportVersion/useRingVersion`，处置后实时联动。（业务线表 / 双指标趋势 / 评分分布 / 队列健康度等聚合「今日」量级数仍为设计态 mock，与样本列表不同尺度、不构成矛盾。）

## 二·补、运营总览（`pages/OpsOverview.tsx`，`/ops`，监控运营组首项）
告警队列的「运营脉搏」专页 —— 仪表盘那张摘要卡的完整展开。数据全部取自 `lib/opsMetrics.ts`（`QUEUE` 完整画图 + `queueHealth()` 判定），与仪表盘同源同口径。
- **队列状态判定条** + **4 格体征**（待处理积压 / SLA 达成率 / MTTR / 超 SLA 件）。
- **燃尽趋势**：积压余量曲线，从今日 `open` 倒推每日值,与 `trend.newD/clearedD` 自洽；标峰值→现值→已燃尽量。
- **停留时长 donut**：按等待时长分段（0–4h / 4–24h / 1–3d / 超 SLA），超 SLA 红；累积偏移在渲染期无变异预计算（不在 JSX 闭包里 `+=`，过 react-compiler lint）。
- **分析师负载**：复用 `AnalystLoad`（Top 6 柱 + 「+N」折叠 + 浮窗）**之上**再加**全员负荷明细表**（`QUEUE.analysts` 全 16 人按负荷降序：手头 / 上限 / 利用率条 / 余量·超额状态），把图表只露 Top6 的盲区补全 —— 这是本页相对仪表盘摘要的增量价值。
- **队列下钻 · 改派（总管派单视角）**：点任意人（柱 / 明细表行）→ 打开 **`components/AnalystQueueDrawer.tsx`** 右侧抽屉，列出该分析师手头的 告警 / 案件 / 报送（超时在前；演示态按确定性种子合成的占位队列，接后端应按 assignee 拉真实记录），可逐件「改派」给有余力的同事（`spareAnalysts` 按空余量降序，排除本人），超载时「**一键分流**」把所有超时件轮转分给有余力的人。`AnalystLoad` 加可选 `onSelect` 触发下钻；仪表盘总管视角「团队负荷」卡共用此抽屉。`analystQueue`/`spareAnalysts`（`lib/opsMetrics.ts`，纯函数同输入同输出，重渲染不乱跳）。
- 入口：侧栏「运营总览」（`Activity` 图标，监控运营组首项）+ 仪表盘队列健康度摘要卡整卡点击。

## 三、交易监控流水线（核心模型）
一笔命中规则的交易，按生命周期在四个页面间**串行流转**，**任一时刻只属于一个页面**。

### 车道模型（按 `state` 区分，定义在 `lib/data.ts`）
- `GATE_STATES = [new, pending]` → **事中监控**（实时闸口车道）。
- `INVESTIGATION_STATES = [progress, pending_l2, escalated]` → **告警研判**（调查车道）。
- `closed_*` → **处置记录**（归档）。
- 状态机仍是 `RC_STATES`；状态 / 经手人 / 事件存 `alertStore`（内存，刷新重置，无后端）。

### 先后顺序
- **实时交易命中规则** → 先进 **事中监控**；当场拍不了板 →「转研判」→ 进 **告警研判**。
- **事后 / 批量 / 团伙检测** → **直连告警研判**（无事中环节，钱已动）。
- 不是每笔都会进告警研判：事中能当场放行 / 拒绝的，直接进处置记录。

### 四个页面
- **事中监控**（`pages/Monitoring.tsx`，`/monitoring`）：回答「这笔钱放不放」。实时指标条 + 充值 / 提现 Tabs + 告警样式表格（按 SLA 紧迫度排序）。行操作 = 查看(进详情) / **审核(行内开 `ReviewDialog` 抽屉)** —— **三个监控列表(事中 / 事后 / 告警研判)统一为行内抽屉审核**。
- **告警研判**（`pages/AlertList.tsx` + `AlertDetail.tsx`，`/alerts`，**原「交易警报」改名**）：回答「这单可疑吗、要不要转合规上报 STR」。只显示调查车道；tiles = 调查中 / 待 L2 复核 / 已升级；每行 **来源徽标**（实时升级 = 事中转研判 / 事后检测 = 回溯直连）。**事中 / 告警详情(`AlertDetail`,info tab,两车道共用)带「关联线索·跨模块」卡**:除已有的关联团伙(`ringsForMerchant`)外,自动按主体匹配**同主体的事后命中 + 案件**(可跳转),提示升级 / 转研判 / 建案时并入同一案件,避免重复立案 / 重复 STR —— 让关联视图在四个监控页(事中 / 事后 / 告警 / 案件)全覆盖。
- **事后监控**（`pages/PostMonitoring.tsx`，`/post-monitoring`）：交易完成后的回溯 / 批量检测（结构化拆分、扇入归集、休眠突发、币币链跳、速度骤增、对手集中度）。两层状态：
  - **批次状态**（轻量）：扫描汇总卡上显示 `批次号 + 已完成 + 完成时间 + 下次计划`，不做成重表（分析师关心命中项,不关心批次）。
  - **命中项状态机**（分析师天天碰的主体，似告警但有事后特有项）：`新建·待认领 →(认领)→ 处理中 →(补料 RFI / 升级 L2·MLRO)→` 终态。终态比事中多两种：**已结·确认可疑·转报送**（补 STR）、**已结·转案件**，外加 **已结·误报**。状态 tiles + 状态列 + 按状态门控操作；终态处置走**处置弹窗**。
  - **事后特有两维度**：**⑧ 已出账·需追溯** —— 资金已不可逆流出（事中还能冻结，事后不能），确认可疑时必填**资金追溯评估**（可追溯·冻结下游 / 部分 / 不可追溯·上报损失），并打「已出账·需追溯」标记；**⑨ 规则回填** —— 结案时可把该 typology 回填检测规则（默认勾选），让事中即时拦同类（事后 → 规则优化闭环，事中没有这条）。
  - **审核：列表内联 + 详情页两条路径**（与告警研判一致）。列表（`PostMonitoring.tsx`，含 SLA剩余 / 分配给 列）行操作**按状态自适应**：`新建·待认领`→「认领」（直接认领）、其余 active 状态→「审核」（**列表内联打开 `FindingReviewDialog` 抽屉**，无需先进详情）、终态→仅「查看」。眼睛图标始终进 **`pages/PostDetail.tsx`（`/finding?id=`）** 看完整研判工作台（详情页顶部也有同一个「认领 / 审核」入口）。**处理中 → 确认可疑 → 追溯中** 即在该抽屉内完成。命中数据 `lib/findings.ts`，状态 / owner / 追溯 / 冻结 / 损失 / 回填 / 事件存 `findingStore`（`lib/store.ts`，内存，跨列表↔详情持久）。
  - **审核走右侧抽屉**（与告警研判一致）：详情页顶部 `new` 状态显「认领」、其余 active 状态显「审核」按钮 → 打开 **`components/FindingReviewDialog.tsx`**（右侧抽屉，样式 / 宽度同 `ReviewDialog`）。抽屉按命中状态自适应：**处置结论**（确认可疑 / 转报送 / 转案件 / 误报关闭）+ **流程操作**（补料 / 材料已回 / 升级）选择卡 + 操作后影响 + 调查依据；**终态结论(转报送 / 转案件 / 误报关闭)必填结构化「处置理由」**（`FREASONS`，记审计日志，与告警 `ReviewDialog` 一致）；选「确认可疑」时展开资金追溯评估（必填）+ 规则回填勾选。**资金追溯三档联动追回组处置**（`TRACE_TIERS` / `traceRecovery`，`lib/findings.ts`）：选档位给「追回组」一个**默认建议**(**可追溯·已冻结→默认请求下游冻结**;**不可追溯→默认上报已发生损失**;**部分可追溯→不预设**)。**默认建议不互斥、不禁止**——只「加」对应默认动作、不清除另一项,冻结与损失可并存(如可追溯但部分已流失),另一项仍可在追溯工作台手动补登;**止损组(封号 / 列名单 / 规则回填)保持手动**。**详情页顶部不再有一排内联动作按钮**；`追溯中` 状态顶部显 **「追溯处置」(主)+「结案审核」(次)** 两个按钮:追溯处置开 **`components/TraceDrawer.tsx`** 右侧抽屉(同款样式),结案审核开 `FindingReviewDialog`(转报送 / 转案件)。
  - **详情页是研判工作台**（够给结论）：命中详情(含 **检测维度** —— 事后是批量回溯,主体维度不统一:`FDIM` **商户主体 / 链上地址 / 多商户网络**,如扇入归集 `030` 是 6 商户→同一地址的多商户网络,链跳 `028` 是地址维度;列表主体列**带维度徽标**(图标 + 标签,**配色已转中性灰** —— 按全站「只有状态用色」口径,维度是分类不上色;`FDIM.tone` 字段保留但不再用于徽标)、列表有**检测维度筛选**(全部/商户/地址/网络)、详情命中详情与「主体画像」标题均按维度自适应,画像随之为 商户/地址/网络画像)+ **风险信号**(factors) + **为什么事中漏判·系统建议**（规则缺口 → 处置建议**合并为一张卡**，规则缺口标黄、系统建议浅底框）+ **资金路径**（逐跳流向图：节点带**角色** 来源/归集/中转/混淆/跨链/出口/失联 + 角色图标 + 风险配色 + 每跳金额/说明，连线按下游风险上色；数据 `PathNode[]`，**按 typology 自适应**——仅扇入归集 `030` / 链跳混币 `028` 有，拆分/速度/集中度类不显示）+ **涉及交易明细表**（构成该模式的逐笔，含 **交易ID / 哈希** 列(按维度自适应)：商户维度=订单号(拆分 `031` DEP / 休眠突发 `029` WD)，**链上地址维度=交易哈希**(链跳 `028` `0x7c91…`)，归集/汇总类逐笔不适用显「多笔汇总」，标题按是否逐笔自适应「涉及交易明细 · 逐笔样本」/「资金流构成 · 按环节汇总」，资金流类底部加注脚说明各环节是同一资金不同阶段、非加总）。**对手集中度类(`026`)用占比条**（`DistSeg`，金额占比横条 + 图例,替代逐笔表格）。**链上地址 / 网络维度的画像带地址风险标签**(`profile.tags`，如 隐私币兑换 / 跨链桥 / 溯源切断 / 扇入归集 / 高风险，类比告警 addrIntel)+ **主体画像**（右栏）+ 活动日志。**处置摘要**改为**顶部横向状态条·常驻**（当前状态 / 资金追溯 / 账户处置 / 对手名单 / 下游冻结 / 损失上报 / 规则回填，按状态上色，空值占位「— / 未评估 / 未回填」；`追溯中` 带「管理追溯处置 →」链接）。详情证据 `DETAIL`（`lib/findings.ts`）。
  - **关联命中 · 跨维度闭环**(`Finding.related`):同一笔钱被不同维度镜头各看一遍 —— 同一主体 / 网络可在多维度同时命中(如 RapidPay 既在 `031` 商户拆分、又是 `030` 扇入网络的资金来源)。详情页「关联命中」卡列出该主体在**其它维度**的命中(配色徽标 + 状态 + 可跳),并提供 **「一并转入同一案件」一键并案** → `caseStore.add` 生成案件、**涉案主体按维度自动并集**(`dimSubjType`),各命中记「并案 → CASE-xxx」事件。**减少人工**:免去手动找关联 + 手动建案 + 重复 STR。
  - **追溯处置抽屉**（`components/TraceDrawer.tsx`，`追溯中`状态）：状态徽标 + 资金追溯评估(带档位联动) + 止损(限制/封禁 · 对手列名单 · 规则回填)+ 追回·留痕(请求下游冻结 · 上报已发生损失)+ 去监控规则链接;**动作即时生效**(非提交式),底部「去结案审核」可切到 `FindingReviewDialog`。`规则回填`带「操作后影响」（→变更治理→回测→审批→事中拦截，生效前不影响线上）。
  - **批次历史**：扫描汇总卡「批次历史」→ 右侧抽屉，近 6 批（区间 / 扫描量 / 命中 / 确认可疑 / 误报 / 状态）。
  - **确认可疑 → 转案件**(不再「转报送」直连):事后处置去掉「转报送·STR」选项,确认可疑统一**转案件**(`FindingReviewDialog` 选「转案件」→ `intakeCase`,来源=事后转案件,同主体在办则并入),STR 由案件统一起草。误报 / RFI / 升级照旧。
  - **转案件两条路径**(`STEP_FLOW`):默认 `处理中 →(确认可疑)→ 追溯中 →(评估追回/损失)→ 转案件`(资金已出账,追溯结论是 STR 证据);**`处理中/待补料/已升级` 也放开「转案件」直达** —— 已看清需多主体深查时免走单条命中的追溯。
- **处置记录**（`pages/Dispositions.tsx`，`/dispositions`）：**全口径处置归档**（已放行 / 已拒绝 / 加入名单 / 转合规 / 误报关闭），结果分桶 tiles + 表格，只读，带**来源**标记（事中 / 告警研判 vs 事后监控）。**实时合并**：本会话已决策告警（`closed_*`，「今日」绿标）+ **事后监控已结案命中**（来源=事后监控，点击进 `/finding`）都汇入此处归档。**点击查看**:有关联原始记录的跳 `/alert`、`/finding`;静态归档记录开**只读详情抽屉**(归档字段 + 处置原因)。
- 导航顺序（`Shell.tsx` 监控运营组）：运营总览 · 事中监控 · 事后监控 · 告警研判 · 处置记录 · 主体档案。

### 审核操作：统一在详情页，`ReviewDialog` 按车道自适应
`AlertDetail` 顶部「审核」打开 `components/ReviewDialog.tsx`（右侧抽屉），动作集按车道切：
- **gate（事中）** → 放行决策：**放行 / 拒绝 / 补料 / 转研判**（`GATE_DISP` / `GATE_PROC`）。**「转研判」原因必填**（`REASONS.transfer`，记审计日志）。
- **investigation（告警研判）** → 放行结案 / 建案 / 加入名单 / 请求信息 / 升级 L2（原集 `DISP` / `PROC`）。
- 状态映射 `SUBMIT`：release→`closed_done`、reject→`closed_case`、reqinfo→`pending`、**transfer→`progress`（gate→investigation，自动离开事中、进告警研判）**、case→`closed_case`、watch→`closed_done`、l2→`pending_l2`。
- `AlertDetail` / `Monitoring` 的面包屑与返回按车道切（事中监控 ↔ 告警研判）。

## 四、关联团伙模块（`pages/RingList.tsx` / `RingDetail.tsx`，已完成）
数据 `lib/rings.ts`；状态 / owner / 事件存 `lib/store.ts` 的 `ringStore`（内存）。
- **识别原理**：共享标识连成加权边 → 剔除超级节点 → 累积强度过阈值 → 图聚类 → 多维打分。区分度加权 **资金 40 > 地址 30 > 设备 20 > IP 10**；阈值 **60%**（≥60% 成团 / <60% 仅观察中）；置信度 = 各维 contrib 之和（数据里 confidence 必须等于 shared.contrib 之和）。
- **状态机**（`RING_STATES` + `ringActions` 门控）：`待认领→(认领)→调查中→(处置)→终态`；`观察中` 弱关联旁路（只能升级 / 误报）。处置→状态见 `DISP_STATE`。
- **关系图谱**：≤5 人单环；≥6 人 **力导向布局** `forceLayout`（FR 斥力 / 引力 + 向心，确定性无随机，`useMemo` 稳定，bbox fit）。大团伙样本 `RING-2026-082`（12 主体 / 14 连线）。边色 = 维度、粗细 = 强度。
- 详情页：团伙详情（图谱 + 共享证据 + 置信度构成堆叠条 + 成员）/ 活动日志两 tab；研判处置抽屉（门控过滤 + 操作后影响）。`components/RingBasis.tsx` 识别依据气泡。
- 术语：**连线**=边、**项**=连线上的证据实例数、**强度**=单连线、**置信度**=团伙累计分。

## 五、报告报送（FINTRAC 合规上报 · `pages/ReportFiling.tsx`，`/reports`）
**MLRO 视图**，全口径合规上报工作台。数据 `lib/reports.ts`；状态 / MLRO 经手 / 回执 / 事件存 `lib/store.ts` 的 `reportStore`（内存）。
- **报告类型（加密 MSB 口径）**：`STR` 可疑交易报告（确定可疑后 30 日内）/ `LVCTR` 大额虚拟货币交易报告（≥CAD 10,000，交易后 5 工作日，**客观阈值、系统自动、无需可疑判定**）/ `TPR` 制裁 / 恐怖分子财产报告（立即·无延迟）。
- **STR 唯一发起点 = 案件管理**（核心架构）：所有可疑/违规线索(事中驳回 / 告警升级 / 团伙命中 / 事后命中)先汇成**案件**,STR **只在案件里起草**,案件进 `str_draft/mlro/queued/filed` 后由 `ReportFiling` **实时派生 STR**汇入此处(`caseStrState` 映射状态;来源标「案件管理」可跳 `/case`)。**不再有 告警转合规 / 事后转报送 直连 STR**。**LVCTR**(系统按客观阈值)/ **TPR**(制裁命中即时,非可疑判定)仍系统直连。报告报送退回为**纯 MLRO 报送台**(复核→报送→回执)。
- **报告状态机**（`RSTATE` + `RFLOW` 门控）：`草稿 →(提交)→ 待MLRO复核 →(复核通过)→ 待报送 →(报送)→ 已报送·待回执 →(回执)→ FINTRAC已接收`；`被退回·需补正` 旁路（FINTRAC 退回 → 补正后重新复核 / 作废）；`已作废` 终态。
- **页面**：需立即处理条（退回需补正 / 待复核 / 已报送待回执 / 在办，时限驱动）+ 状态分桶 tiles + 类型筛选 chips + 搜索 + 报送台账表 + **「新建报告」**按钮。报告全集口径统一在 `lib/reportsAll.ts`（`caseDerivedReports`=案件派生 STR + `reportStore.created()`=手动新建 + 静态 LVCTR/TPR；`allReports` / `findReport` / `liveStatus`），列表与详情共用。**列表行点击 / 眼睛 / 复核都进整页详情 `/report?id=`**（不再开抽屉看摘要）。
- **报告详情整页 · 按类型自适应**（`pages/ReportDetail.tsx`，`/report?id=`，对标用户 mockup）：页头卡（编号 + 类型/状态徽标 + 报送时限胶囊 + 预览报文 + **类型化主操作**：STR「MLRO 签发报送」/ LVCTR「批量报送」/ TPR「上报·回执」→ 开 `ReportDrawer` 决策）+ **类型化进度条**（`RSTEPS`/`reportStep`：STR=草稿→分析师复核→待MLRO签发→已报送→受理；LVCTR=阈值触发→自动归集→待报送→…；TPR=制裁命中→资产冻结→…）。**STR/LVCTR 详情分叉**（核心）：① **sub 行** STR=涉事主体/来源案件/起草人/金额，LVCTR=交易批次/系统自动归集/触发阈值/笔数；② 左栏 STR/TPR=**可疑理由/上报依据叙述**（人工撰写 + 风险标签胶囊），LVCTR=**归集交易明细表**（批次逐笔 + 合计 + 客观阈值横幅，无需可疑判定）；③ 右栏「报告信息」STR=监管口径/来源案件/起草人/签发人，LVCTR=触发方式/阈值/批次笔数/合计；④ **FINTRAC 报文预览**：**STR 走完整分部报文** `StrDoc`(对标 FINTRAC F2-R)——Part A 报告与报送机构 / B **交易明细·虚拟货币**(币种·CAD折算·汇率·发送/接收地址·交易哈希·确认数,存入/提出分卡) / C 账户 / D 涉事主体(实体+UBO) / F 可疑理由·合理怀疑(叙述) / G 已采取措施,中英双标签、风险字段高亮;**LVCTR/TPR 走简版键值**(batch / immediate)。+ **处理记录**（类型化时间线 + 本会话实时事件）。详情数据 `ReportDetail`/`RDETAIL`（STR 全量分部报文,其余 STR `synthStrDoc` 兜底）（hero：`STR-CASE-20260318-001` NovaPay 叙述、`LVCTR-20260619-0143` 12 笔批次、`TPR-20260313-0021` OFAC）+ `reportDetail()` 按类型兜底合成。
- **新建报告抽屉**（`components/NewReportDrawer.tsx`）：类型三选（STR/LVCTR/TPR，各带**判断指引**——STR 通常由案件派生、此处补录例外；LVCTR 客观阈值无需可疑理由；TPR 立即上报）+ 主体/金额/来源 + 按类型自适应理由（STR/TPR 必填可疑理由/依据，LVCTR 免填）。`reportStore.add` 落库：STR→草稿、LVCTR/TPR→待报送，记审计事件。
- **MLRO 复核 / 签发抽屉**（`components/ReportDrawer.tsx`，右侧抽屉，**重做为像样的 MLRO 签发台**）：顶部 **MLRO + 法定时限**（STR 30 日紧迫度 pill）+ 报告摘要（类型/状态/主体/金额/起草人/来源链接/回执）+ **复核重点按类型自适应**：STR/TPR 显**可疑理由叙述·合理怀疑（Part F）**（取 `reportDetail` 的 `strDoc.grounds`/`reason.body` + 风险标签 chips，MLRO 真正要审的）；LVCTR 显**客观阈值横幅**（无需可疑判定，复核重点为金额折算/对手信息）+ **FINTRAC 必填字段·完整性核验**（可勾选 ✓齐备 / ⚠待确认 + 「齐备 N/M」计数，非装饰复选框）+ **状态门控处置动作** + **法定签发声明**（签发 `→queued` / 报送 `→filed` 前必勾 attestation：确认已复核必填字段、报文真实完整、符合 PCMLTFA；字段未核齐则拦截并提示走「退回补正」）+ **负向动作理由必填**（作废 `→void` / 退回起草人 `→draft` / FINTRAC 退回补正 `→returned` 必须填理由留痕,`needsReason`,标签随动作自适应「作废理由 / 退回理由」,未填则按钮禁用 + 报错)+ 复核意见。报送 / 回执落 MLRO 经手 + 生成 FINTRAC 回执号。**详情页主操作即调此抽屉**（整页证据 + 抽屉决策，与案件 `CaseReviewDrawer` 同范式）。
- **实时闭环**：`[...caseStore.created(), ...CASES]` 中状态 ∈ `str_draft/mlro/queued/filed` 且非「制裁规避」的案件 → 实时派生 STR(`STR-${caseId}`)汇入此处,MLRO 在此驱动 复核→报送→回执;案件 `strLabel` 映射 STR 进展可回跳 `/case`。

## 六、监控规则与规则回填闭环（`pages/RulesPage.tsx`，`/rules`）
检测规则库 + 变更治理。数据 `lib/rules.ts`；状态 / 经手 / 事件存 `ruleStore`（`lib/store.ts`，内存）。
- **规则状态机**（`RUSTATE` + `RUFLOW` 门控）：`回填/提案 → 回测中 →(回测达标·提交审批)→ 待审批 →(审批上线)→ 已上线·生效` / `已驳回` / `已停用`(可重新启用)。
- **类别**(`RuCat`)：金额阈值 / 链上溯源 / 行为模式 / 名单筛查 / 评分模型 / 统计聚合，各带图标。
- **执行场景**(`Venue` = 事中 / 事后 / 两者，列表 + 详情显场景徽标)：决定规则能否实时拦截 —— **事中**=交易发生时即可算出(单笔阈值 / 名单 / 地址 / 同主体滑窗累计);**事后**=需跨时间 / 跨主体聚合,实时算不出(扇入 / 速度 / 集中度 / 链跳回溯);**两者**=事中近实时 + 事后复扫(链上溯源 / 扇入图关联)。`venueOf` 默认:统计聚合→事后,其余→事中。
- **新建规则**(`components/NewRuleDrawer.tsx`，**借鉴 Fireblocks TAP 策略编辑器范式重做**):名称 / 类别 / **执行场景** / **结构化条件 IF…THEN…ELSE** / 权重;新规则**不直接上线**,落「回测中」(`ruleStore.add`,内存)→ 回测达标 → 审批 → 上线。回填规则也带场景(回填目的就是让能实时的进**事中**拦同类)。**五项 Fireblocks 化优化**:① **典型模式快速起步**(`RULE_TEMPLATES`,6 个 typology:大额单笔 / 结构化拆分 / 多账户扇入 / 休眠后突发 / 对手集中度 / 币币链跳)—— 点模板一键预填名称 / 类别 / 场景 / 结构化条件 / 处置 / 权重(对应 Fireblocks 模板化配置;从空白 → 有起点,与回填闭环 typology 对齐);② **结果优先的处置类型卡**(对应 Fireblocks ALLOW / BLOCK / 2-TIER):**拦截·冻结 / 评分·转研判 / 评分·加强监控 / 待补料·核验** 四张卡选其一,处置是头等选择而非平铺下拉;③ **评分类内联权重** —— 只在选「评分」类处置时出现权重输入(必填),由处置类型 + 权重派生最终 action 文案(`评分 +40 · 转研判`),拦截 / 补料类不需要权重;④ **智能默认**(对应 Fireblocks 通配 / 合理默认):选类别**自动建议执行场景**(`venueOf`,未手动改过才建议)+ 条件值按字段给**单位 / 占位提示**(`FIELD_HINT`:金额→CAD、评分→0–100、窗口→笔 / 天);⑤ **回测预估**(对应 Fireblocks「规则即一句话」):条件就绪后给**近90天预估命中 / 误报**量级(演示态确定性派生,真实重放走「回测模拟」调阈值)。注:Fireblocks 的 **first-match-wins top-down** 评估模型与本系统的「全局策略·最严生效」不同,本次不改评估模型,仅借编辑器范式。
- **新建 / 编辑规则界面 = 卡片化两栏布局(对标用户 mockup `监控规则DEFAULT.png` 重排)**:居中全屏 Modal(`components/NewRuleDrawer.tsx`,导出名沿用),原 ClickUp 两栏被**重排为「头卡 + 虚线连接 + 内容子卡」**的卡片层级,把原本平铺、拥挤的字段拆成各自独立的圆角白卡,左右两栏各自从「列头卡」垂直串下来:
  - **顶部内联命名** + 副行说明(不变)。**原全宽规则元数据行已撤** —— 折进左栏(见下「事件类型」卡)。
  - **左栏「触发条件」**(`ListFilter` 头卡):**执行场景(事中/事后/两者)从图标按钮组改为头卡右上角下拉**(`Select`,带 venue 图标 + `short` 标签),选中后头卡下方一行 venue `hint`(选类别自动建议时带「(按类别建议)」)。头卡 →(虚线)→ **「事件类型」子卡**(原规则元数据折入此处):**规则类别**(全宽,带 `Layers` 图标)+ **适用场景 / 适用网络**(两列,各带图标)+ **规则描述**(选填 `Textarea`)。→(虚线)→ **条件标题行**(「当交易满足以下全部/任一条件」+ 右侧 **AND/OR 胶囊切换**,选中态蓝底白字)→ **每条条件一张「条件 N」子卡**(顶部标题 + 删除;**运算字段全宽** + 下一行 **运算符 / 值**两列,金额字段带 `$…CAD`),取代原拥挤的单行三列;末尾 **圆形「+」加条件按钮 + 运行规则**(居中)。**移除了原内联「预览规则」框 + 假拖拽手柄 + 添加子组**(底部成品句子条已是实时预览)。
    - **右栏「命中动作」**(`ShieldCheck` 头卡,右上「命中后自动执行」)→(虚线)→ **「事件操作」子卡**:**仅评分 / 生成告警 单选**(`RadioGroup` 横排,取代原双图标卡)+ 一行随选项变化的说明 + 分隔线下 **「如果交易匹配此规则,则停止扫描交易」勾选**。→(虚线)→ **「动作执行」子卡**:**处置动作单选**(`RadioGroup`,`RULE_DISPOSITIONS` 按严格度降序;`仅评分` 模式禁用;或勾⑥阶梯处置切档位表)+ 分隔线下 **风险权重滑块(0–100)**。**处置 = 单一终态结果**(一笔交易只能落到一个最终状态:拒绝/冻结/补料/人工/记录),不再多选 —— 多选会把互斥终态(冻结 vs 入账)和可叠加副作用混在一起、且「取哪个」无定义。**`DISPOSITION_RANK` 为唯一真值来源**:跨规则**最严生效**(多条命中取严格度最大的终态)、旧多选规则编辑时按 `strictestDisposition` 收敛为单选,都引用它。**已删除「升级路径」「严重度」两项** —— 二者原为 write-only 字段(抽屉收集但全站无处读回:详情页「升级路径」由风险等级派生、不读 `rule.escalation`,「严重度」也从未读 `rule.severity`),且与 `升级路径` 概念跟处置/权重重复。升级走处置选项「冻结资金并升级 MLRO」、严重度由风险分/权重隐含,跨规则裁决靠「最严生效 + 权重聚合」,无需规则级单设。
  - **中栏**:箭头芯片(顶部对齐,连接两列头卡)。
  - **底部常驻成品句子条**:`当 ⟨条件 token⟩ 则 ⟨处置⟩ 权重 ⟨+N⟩`,滚动时始终可见,作实时规则预览。
  - **页脚**:运行 30 天回测 / 保存并提交。
  - 数据:`Rule` 元数据(`scope/network/desc/mode/stopScan/actions/joiner`,向后兼容),提交仍派生 `action`/`weight`/`cond`。布局原子 `CardHd`/`SecCard`/`Connector`。(原 `escalation`/`severity` 字段已删,见上。)
- **变更治理 · 改已上线规则走「拟议变更 + 总管审批」(核心)**:编辑一条**已上线·生效**的规则**不直接动线上** —— `NewRuleDrawer` 提交时落一份**拟议变更**(`ruleStore.proposeChange`,完整字段快照 + 提交人 + 摘要),原版**照常拦截**,记一条「待审批」事件。规则详情页顶部出**拟议变更横幅**:列「线上现版 → 拟议」的字段 diff(`ruleFieldDiffs`,触发条件 / 权重 / 处置 / 名称),**风控总管**(Emma Zhang)可**批准上线**(`approveChange`:拟议字段套到线上 `ruleEdits` + 记一个新版本 + 事件,清空 pending)或**退回**(`rejectChange`,理由必填,丢弃改动、线上维持现版)。**门控分级**:`已上线` 规则编辑按钮变「提议变更」、提交即审批;**未上线**(回测中 / 待审批 / 已驳回 / 已停用)规则**原地直改**(`ruleStore.update`,即时生效);**名单筛查类硬规则**(法定·命中即拦)编辑**锁定**(`Lock` + Tooltip,灵敏度调整走名单管理改数据侧)。审批入口同时挂在仪表盘总管「待我审批」(「规则变更」类,来自 `ruleStore.allPending()`,深链规则详情)。**禁用 / 启用**仍即时(安全闸/急停,不走审批)。把状态机 / 待审批 / 版本历史 / 闭环串成真闭环 —— 解决「创建有治理、修改裸奔」的漏洞。
- **结构化条件构造器**（`Clause` = 字段 + 运算符 + 值 + **窗口**;`clauseText`/`condText` 生成可读条件)：`IF` 多条子句 **AND 连接**(可增删)→ `THEN` 命中处置 → `ELSE` 否则(选填,默认放行 / 继续监控)。字段含 单笔金额 / 滑窗累计金额 / 滑窗笔数 / 对手集中度 / 扇入主体数 / KYW / 风险评分 / 休眠天数 / 地址标签 / 名单 / 跨链 / KYB 等;运算符 ≥ ≤ > < = ≠ 命中 包含。详情抽屉以 IF / 且 / THEN / ELSE 徽标渲染条件逻辑;内置规则无 `clauses` 时仍显条件文本。
  - **适用对象:规则定向到商户分群(不是所有商户都跑所有规则)** —— 规则元数据卡新增**「适用对象」多选**(`RULE_AUDIENCES`,空 = 全部商户):**派生型群组**(高风险商户 风险分≥80 / 新户·KYB 未完成 / 高风险辖区 / 白名单除外 —— 系统按结构化字段实时算,不靠打标签)+ **业务模式标签**(OTC·交易所 / 加密 ATM / DeFi·跨链 —— 算不出来、靠开户分类,这类才值得打标签)。选定后仅命中所选群组的商户触发,实现「不同商户跑不同规则子集」。**法定核心(制裁筛查 / LVCTR)对所有商户恒生效,不受此限**(卡内 `Info` 注明)。`Rule.audience?: string[]`(向后兼容),`audienceText` 派生文案,`RuleDetail` 规则概要加「适用对象」行。打标签原则:**能派生的(风险/KYC/辖区/名单)别打标签**(会过期漂移),只给**派生不出来的**(业务模式 / 临时调查群组)打。
  - **① 滑动时间窗口:指标与窗口解耦(核心反洗钱场景)** —— 把「指标」和「窗口」拆成条件行上**两个独立配置维度**,不再把窗口焊死在字段名里(旧版只有 `7日累计金额 / 24h笔数 / 30日对手集中度` 三个固定窗口,配不出任意滑窗)。窗口型指标(`WINDOWED_FIELDS`:滑窗累计金额 / 滑窗笔数 / 对手集中度 / 扇入主体数)选中后,条件卡内出现一行**滑动窗口选择**(`Clock` + `Select`,`WINDOW_OPTS` = 1 小时 / 24 小时 / 48 小时 / 7 天 / 14 天 / 30 天),其余即时 / 单笔指标无窗口。`Clause.window` 必填于窗口型指标(`clauseOk` 校验);`clauseText` 给窗口型子句加「⟨窗口⟩内」前缀(底部句子条 / 详情 IF 表达式 / 回测台同步),`RuleDetail` 条件表加**滑动窗口列**。**直接解锁头号 typology**:结构化拆分规避 LVCTR(48h 内 累计 ≥$9k 且 笔数 ≥5)、速度突增(24h 内笔数 ≥N)——`RULE_TEMPLATES` 的结构化拆分 / 扇入 / 休眠突发 / 对手集中度模板已改用「指标 + 窗口」表达。
  - **② AND/OR 一层嵌套子组:一条规则覆盖多条独立可疑路径** —— 把旧版单层 AND/OR(及曾经点了只弹 toast 的「添加子组」)升级为**真实一层嵌套**:`groups[gi].clauses` **组内**以 `group.joiner`(AND/OR)连接,**子组之间**以 `outerJoiner`(AND/OR)连接,可表达 `(单笔≥$9k 且 账户<30天) 或 (24h 笔数≥5 且 集中度≥80%)`,不必拆成多条规则(避免命中归因碎裂 / 停止扫描语义混乱)。`ClauseGroup` + `groupText`/`groupsText`(多子组自动加括号渲染,单子组退化为普通 `condText`)。**UI**:单子组时沿用原样(条件标题行内联 AND/OR);**点「添加子组」**后每组套一层卡 + 组头(子组号 + **组内 AND/OR** + 删除子组),组间出现**子组连接器**(居中 AND/OR 胶囊 + 「子组之间」),`JoinerToggle` 子句内 / 子组间共用;底部句子条按子组加括号 token 实时预览。**数据**:`Rule` 加 `groups?`/`outerJoiner?`(向后兼容);提交时单子组只存 `clauses`+`joiner`,多子组才存 `groups`+`outerJoiner`,`clauses` 仍存扁平全集供下游/回测复用,`cond` 存带括号文本。`RuleDetail` IF 表达式优先按 `groupsText` 渲染分组逻辑。
  - **③ 分层阈值:一条规则按段位多档值(减少近重复规则)** —— 条件值可按维度(`TIER_DIMS`:KYC 等级 / 业务线 / 注册地)取**不同阈值**:如 `单笔金额 ≥ 按 KYC 等级分档(VIP:50,000 / 新户:5,000 / 默认:10,000)`,不必为「VIP $50k 正常、新户 $5k 就拦」「off-ramp 比 on-ramp 严」写一堆近乎重复的规则。`Clause.tiers`(`{dim, rows:[{key,value}]}`),`TIER_KEYS` 按维度给候选档位;数值字段勾「按…分档取阈值」即把单值输入换成**分层维度 + 档位表**(档位/阈值行可增删),`clauseText`/详情表/句子条渲染分档。**仅绝对值基准开放**(相对基线已按主体自适应,再分档冗余 —— 切到相对基准自动收起分层);校验放宽为「至少一档完整」(`hasValue`)。
  - **⑤ 上线策略:影子模式 + 灰度 + 到期日(规则级,呼应治理链)** —— 新建/编辑抽屉右栏底部加「上线策略」卡:**影子模式**(`shadow`,只告警不处置的评估期,跑一段确认误报率再切实拦)/ **灰度比例**(`rollout` 10–100%,小流量试跑 → 全量)/ **到期日**(`expiry`,到点自动失效,临时加严如交易所被盗 / 新制裁用)。`Rule` 加 `shadow/expiry/rollout`(向后兼容);`rolloutBadges` 派生徽标,`RuleDetail` 页头状态徽标旁显示「影子模式 / 灰度 N% / 到期 日期」。**纳入变更治理**:`ruleFieldDiffs` 增「处置模式 / 灰度比例 / 到期日」三项 —— 改已上线规则的上线策略同样走总管审批,不裸奔。
  - **⑥ 阶梯处置:动作按金额 / 风险分区间分档** —— 同一规则命中,按金额或风险分**区间触发不同处置**:`≥$9k → 转研判`、`≥$50k → 冻结 + 升级 MLRO`,不再一刀切。`动作执行` 卡右上勾「按金额 / 风险分阶梯处置」(仅「生成告警」模式),把平铺多选换成**分档依据(金额/风险分)+ 档位表**(下限 + 处置动作,可增删);区间为下限、自高到低匹配。`Rule.actionTiers`(`{by, rows:[{from, action}]}`),`actionTiersText` 派生 `action` 文案进底部句子条 / 列表 / 版本 diff;`RuleDetail` 命中动作区渲染阶梯档位表。校验:勾选后需至少一档完整(下限 + 处置)。
  - **⑦ 单笔回放测试:拿真实历史交易试规则(单笔可解释性)** —— 配规则时(条件就绪后)选一笔**结构化样本交易**(`lib/replay.ts` 的 `REPLAY_TXNS`,5 笔:混币器充值 / KYW 超阈提现 / 疑似拆分 / 休眠突发 / 促销正常对照,带数值字段 + 类别标签 + 分层归属 `seg` + 相对基线参照 `base`)点「回放」,对**当前尚未保存的** `groups`/`outerJoiner` 求值:逐子句 ✓命中 / ✗不满足 / —不可判定(`evalClause`,类别走标签匹配、数值走阈值比较、**分层取档按 seg、相对基线用 base 的 mean/σ/P95**),按子组 joiner + 外层 joiner 聚合出**命不命中**,不命中时列出**卡在哪些条件**(`blockers`)。命中时还显示**本规则终态处置**(阶梯模式按本笔金额/分数命中的最高档解析,否则单一终态/评分)+ 一行「跨规则取最严」说明。取代原先只有聚合预估、调规则靠猜。演示态确定性求值,真实重放走后端引擎。`evalRule`/`evalClause`/`combine`(skip 不参与聚合)。
  - **④ 相对基线运算:不只绝对值(减少误报、按异常程度抓)** —— 数值型指标(`isNumericField`,排除 地址标签 / 名单 / 跨链 / KYB 这些只能「命中 / 包含」的类别型字段)新增**比较基准**维度(`Clause.basis`):**绝对值 / × 自身历史基线 / 同业群百分位 P / 偏离均值 σ**(`RULE_BASES`)。条件卡出现「比较基准」下拉,选非绝对值时给一行说明 + 取值框前后缀随基准变(`$…CAD` / `×` / `P` / `σ`,`valStart`/`valEnd`)。`clauseText` 展开为「自身历史基线 ×5 / 同业群 P95 / 偏离均值 ≥3σ」——**复用案件详情已有的 `BaselinePair`「本次 vs 历史常态」与全站「基线偏离 ×N / 同业群 P95」语言**。解锁**休眠突发**(单日出金笔频 ≥ 自身基线 ×5,而非写死固定笔数)、**速度峰值偏离**(> 同业群 P95)等 typology;`休眠激活异常` 模板已改用 `basis:"self"`。回测台滑块对 P / σ 仍可调,对「×基线」诚实无连续旋钮(`parseThreshold` 不匹配 → `Ban` 占位)。
- **内置规则**(9 条,长期 live)+ 治理流水线提案(回测 / 待审批态)。表格列:规则名 / 类别 / 触发条件 / 命中处置 / 近30天命中·误报 / 状态 / 来源 / 负责人 / 操作。状态分桶 tiles + 类别 chips + 搜索 + 顶部**权重校准闭环**条（取代旧「规则回填闭环」概览条）：规则 / 权重从提案到上线再到回填的循环，**5 阶段流**（建模·提案 → 回测 → 审批上线 → 事中生效·误报监控 → 事后回填）每阶段挂**真实计数**（回测中 N / 待审批 N / 已上线 N·最高误报 / 来自回填 N，待审批与高误报标色），宽屏横向带箭头、小屏纵向；末端**虚线回环**绕回「建模」说明误报偏高或事后成案就回调权重重走一圈，并注明 **制裁 / 名单类硬规则不在此环**（命中即拦，不靠权重阈值）。
- **规则回填闭环(核心)**：事后监控「规则回填」(`findingStore.backfill`)的命中**实时派生为规则**汇入此处 —— **历史回填**(静态 `backfill`)显「已上线」(来源=回填自 PM-xxx,可跳回 `/finding`),**本会话新回填**的进「回测中」。`bfrMeta` 按 typology 映射规则名 / 条件(如 结构化拆分→「同主体滑窗累计阈值」、扇入→「多主体扇入同一地址」)。
- **规则详情整页**(`pages/RuleDetail.tsx`，`/rule?id=`，**列表点击进整页,非抽屉**)：页头(状态 + 风险徽标 + 编辑规则按钮 + …菜单)+ **基本信息 / 版本历史 / 活动日志** tab。**操作固定、不随状态变**(与 mockup 一致):页头始终是「编辑规则」+ `…` 菜单(回测模拟 / 复制规则 / 禁用·启用 / 删除规则);状态徽标仍显示但不门控操作。(状态机 `RUFLOW` 仍保留在 `lib/rules.ts`,如需恢复门控可复用。)**操作均有实际流程**:编辑→复用 `NewRuleDrawer`(预填;已上线规则走拟议变更审批、其余 `ruleStore.update` 直改,见上「变更治理」);回测模拟→`RuleBacktestDrawer`(**调阈值看影响**:从规则 cond 抠出真实主阈值[`parseThreshold` 识别 货币 / 百分位 P95 / 比较运算符紧邻数,带单位 + **放宽方向** `lowerLooser`]做成**可拖滑块**,拖动即实时改写 IF 条件 + 重算 命中 / 误报 / 有效率[随阈值的响应为估计模型 `hitAt`/`fpAt`,基线取规则回测样本]+ 画**召回↔精准权衡曲线**[命中 vs 误报随阈值变化,标当前点];布尔 / 名单类**无连续阈值**的规则诚实说明不给假旋钮[`Ban` 占位];选窗口 近30/90/180 天,不改状态);复制→`ruleStore.add` 副本进回测并跳详情;删除→确认 Modal → `ruleStore.remove`(列表/详情过滤);禁用·启用→`ruleStore.set`。`ruleStore` 增 `update/remove/editsOf/isRemoved` + 变更治理 `proposeChange/approveChange/rejectChange/pendingOf/allPending`。基本信息含 **KPI 四宫格**(累计命中 / 待复核 / 误报率 / 升级研判)+ **规则概要(自成一张完整规格表 · 详尽覆盖列表所有列 + `Rule` 全部有效字段,不依赖页头 / KPI)**:可选**规则描述**横幅(`rule.desc`,长文本占整行)+ 17 项键值(**规则编号 / 规则类别[带类别图标] / 当前状态[徽标] / 执行场景[徽标] / 命中权重[+权重 + 风险等级徽标] / 上线策略[影子·灰度·到期徽标,无则「全量实拦·长期有效」] / 触发条件 / 命中动作 / 适用范围 / 适用对象 / 近30天命中[命中数·误报率,未上线显「—·未上线」] / 升级路径 / 来源[回填可跳原命中] / 负责人[头像+名] / 最近更新 / 版本[N 个版本·点击切版本历史 tab] / 数据来源**)—— 与列表(名称·类别 / 触发条件·适用 / 命中处置 / 近30天命中 / 状态 / 来源 / 负责人 / 上线策略徽标)逐列对齐,确保点进详情能看到列表里每一个字段。(原版仅 7 行且 `版本` 硬编码 `v4.0`,已修正为真实版本数;概要扩成完整规格表后,**原「实际效果」进度条卡已删除** —— 误报率已在 KPI 四宫格 + 概要重复出现、命中有效率为 100−误报率 的派生值、升级转案率为硬编码 mock 且与 KPI「升级研判」重叠,删除去冗余。概要改为三列网格铺满整行。)+ **命中趋势**(近30天柱图,近7天红)+ **匹配交易**表 + **触发条件**(IF…THEN…ELSE 表达式 + 字段·运算符·阈值明细表)+ **命中动作与下游处置**流程。活动日志取 `ruleStore` 事件(共享 `Timeline`)。**版本历史 tab**:规则内容(触发条件 / 命中权重 / 处置)的版本谱系 —— `seedVersions` 从当前内容**确定性派生**(初始上线 → 放宽阈值·提升召回 → 当前)+ 本会话回滚 / 变更追加,纵向时间线显每版内容 + **相对上一版的 diff** + 「当前」标,非头版可**一键回滚**(`ruleStore.recordVersion` 追加新版 + `update` 还原内容,**回滚只动内容字段、不动上线状态**)。`RuleVersion`/`seedVersions`(`lib/rules.ts`,含 `parseThreshold`/`spliceThresh` 阈值拼接)+ `ruleStore.ensureVersions/recordVersion`(`lib/store.ts`,首次查看懒初始化不触发通知)。`上一规则 / 下一规则` 在内置目录间切换。
- **闭环路径**：事后确认可疑 → 追溯处置抽屉「规则回填」→ `/rules` 出现规则草案(回测中)→ 回测达标·提交审批 → 审批上线 → 事中即时拦同类(堵下一笔,本笔不依赖追回)。

## 七、案件管理（`pages/CaseList.tsx`，`/cases`）
合规案件全流程。数据 `lib/cases.ts`;状态 / owner / 事件 / 新建存 `caseStore`(`lib/store.ts`,内存)。
- **来源(各动作真正建案 · 经 `lib/caseIntake.ts` `intakeCase`)**:**告警研判「建案」**(`ReviewDialog` choice=case→告警升级)/ **事中「拒绝」**(gate reject→事中驳回)/ **团伙「转案件」**(`RingDetail`→团伙转案件,涉案主体取团伙成员)/ **事后「转案件」**(`FindingReviewDialog`→事后转案件)/ 制裁冻结 / 手动新建(共 6 种)。列表带来源标记,关联项可跳 `/alert`·`/finding`·`/ring`。
- **建案去重 / 按主体聚合**(`intakeCase`):建案前先查**同主体是否已有在办案件**(active 态),有则**并入**(`addSubject` + 记事件,toast「已并入在办案件」),无才新建 —— 避免同一商户被多个告警 / 命中各开一个案件、重复起草 STR(案件 = 单一容器)。
- **案件 = 调查与 STR 唯一漏斗**:案件确认可疑 → 派生 STR 进报告报送;案件结案 → 汇入处置记录(来源=案件管理)。
- **双向联动**:案件派生的 STR(`STR-${caseId}`)在报告报送被 MLRO 推进到 待报送/已报送/已接收 后,**回灌案件详情**顶部提示条(读 `reportStore`),`已报送/已接收` 提示「可结案归档」收尾 —— 解决案件僵在 MLRO 评估的问题。
- **案件状态机**(`CSTATE` + `CFLOW` 门控):`调查中 →(起草STR)→ STR草稿 →(提交)→ MLRO评估 →(通过)→ 待报送 →(报送)→ 已报送 →(结案)→ 已结案`;`调查中` 可直接「无需报送·结案」;任意调查态可「合并到关联案件」→ `已合并`。
- **STR 列 = 关联报告进展**(与案件状态区分,不冗余):`strLabel` 按案件状态映射 未起草 / 草稿中 / MLRO评估 / 待报送 / 已报送,**跳报告报送**;`待报送 / 已报送` 联动 STR 报送闭环。
- **页面**:8 状态分桶 tiles + 风险类型 chips + 搜索 + 表(案件号·来源 / 主体 / 类型 / 优先级 / 金额 / 关联 / STR / 状态 / 分配给 / SLA / 操作)。行操作**按分配门控**:未分配→「认领」、已认领→「审核」。**点击案件号 / 审核 / 眼睛都进整页研判工作台 `/case?id=`(不再开抽屉,`CaseReviewDrawer` 已删除)** —— 原抽屉证据太少不足以支撑审核。
- **整页研判工作台**(`pages/CaseDetail.tsx`,`/case?id=`):**结合用户 mockup 重构为 MLRO 级证据卷宗 + 决策收敛(全宽堆叠布局)**。证据数据 `CaseDossier`(`lib/cases.ts`,`CASE_DOSSIER` 覆盖 5 个代表案件,NovaPay `CASE-20260318-001` 为 mockup 全量对标样本 + `dossierOf` 兜底派生)。页头(状态 / 优先级徽标 + 认领 / STR报送)→ **顶部系统建议处置弱建议** → **研判工作台 / 活动日志** tab。`上一/下一案件` 切换。
  - **系统建议处置(弱建议)** —— 收敛分散信号成决策起点:风险等级 + 建议动作(`rec.disp` 对应决策动作 key)+ 依据 bullets(如混币器接触 + 过水 + 同类 71% 上报)。**「同意建议」**(预选下方处置 + 记审计)/ **「推翻·写理由」**(必填理由 + 记审计)。
  - **风险评分构成 · 加权瀑布**:四因子按权重累积(混币器 `KYW·40%` / 过水 `行为·30%` / 制裁 `名单·20%` / 账户 `账户库·10%`,**权重口径与团伙识别同源**),命中硬指标触发**托底规则**追加分(`floor`,如 81.1 → 87)。瀑布偏移条 + 右轴累计值;下方**因子可展开**看原始分 / 贡献 / 证据依据。`ScoreFactor`(`raw`×`weight`=`contrib`)+ `caseScore` / `factorContrib`。
  - **关联告警表**(`CaseAlert`):优先级 / 告警ID / 描述 / 命中规则 / 触发时间,可跳原始告警。
  - **交易·资金状态** + **相似案件处置基准**(⑤ 决策锚点:STR率 / 限制拒绝 / 放行 三档 + 占比条)。
  - **主体画像与行为基线(多主体切换器)**:案件是**多主体调查容器**,卡顶列出全部涉案主体(类型彩点 chips)可**点选切换**。**主交易主体**(= `c.subject`)给完整画像(KYC / 制裁 / PEP / 30日额·限额)+ **行为基线「本次 vs 历史常态」对照**(`BaselinePair`,偏离标红,如均单 ×7 / 新地址 / 异常时段);**其它涉案主体按类型自适应** —— 链上地址给**地址风险标签**(`addrTags` 从名称 / 角色推导:混币器 / 扇入归集 / 制裁名单 / 中转 / 出口 / 设备聚类)、个人 / UBO 给在案角色与身份。解决「多主体却只展示一个画像」。
  - **涉案主体清单** + **关联案件·串并**(`RelatedCase` 富卡:关联类型 同团伙/同设备 + 置信度 + 关联依据 + 归属人;统计行 关联数/串并总额/最高强度/疑似团伙;**建议纳入**其它维度命中 `intake`)。
    - **关联 ≠ 合并 · 要不要单独审核分档**(回答「关联案件还要不要去各自审核」):**高置信·同团伙** → 标「建议并案」,`并入本案`(`mergeIn`:被并案件转 `merged`、主体折入本案、记审计)→ **一次审核、一份 STR,被并案件不再单独处置**;**中/低置信** → 标「仅关联·各自审核」,默认只作研判佐证、**各自仍独立处置**,确认同伙再升级 `并入本案`;另有 `解除关联`。动作按 `认领 + active` 门控。
  - **证据材料**(`files`:KYT/KYC/流水/OFAC 附件,类型徽标 + 预览/下载)+ **STR 草稿**(`str`:类型/可疑指标/起草人/签发权 + 可编辑叙述摘要 + 保存草稿 / **提交 MLRO 评估**联动 `str_draft→mlro`)。
  - **资金链路分析**(`components/CaseFundGraph.tsx`,`CaseGraph`):**分层有向泳道图** —— 列 源头→分散→多跳中转→归集→平台→出口;**节点用 HTML 芯片 + lucide 图标**(`ICON`:混币器 Shuffle / mule UserRound / 归集 GitMerge / 平台 Landmark / 出口 ArrowUpRight,**与全站及事后监控资金路径视觉语言一致,不用 emoji**),按 `GNodeKind` 配色;SVG 仅画贝塞尔连线(带资金量标注),**链上·法币跨境分界**虚线(`dividerAfter`)+ lucide 图例芯片 + 统计信息行。无 `graph` 的案件回退线性逐跳 `path`。
  - **做出决定 · 处置 → 审核抽屉**(`components/CaseReviewDrawer.tsx`,**与告警研判 `ReviewDialog`、事后 `FindingReviewDialog` 同款右侧抽屉**):页头「审核」按钮(或系统建议「同意」)打开;页面只留一个入口卡(不再内联动作)。抽屉头「案件操作 · {id}」+「{状态} · 处理方 风控 L1」。**三组操作**(`CASE_OP_GROUPS`):**处置决定**(确认可疑 / 确认违规 / 误报放行·需双签)· **流程操作**(请求信息·选材料 / 升级至 L2 / 转 MLRO)· **案件管理**(结案归档·归档原因 / 合并案件·选目标 / 关联案件)。选动作 → **操作影响** + 自适应字段(归档原因 `ARCHIVE_REASONS` / 合并目标 / 需补材料 `MATERIALS` / 双签)+ 处置说明(必填项校验)→ `caseStore.set`;`确认可疑/违规`(→`str_draft`)联动报告报送;系统建议动作带「建议」角标(`recToOp` 映射)。`?rev=1` 可深链直接打开审核。
- **涉案主体清单**(`CaseSubject`,`subjects[]`):案件是**调查容器**,可含**多商户 + 多主体**(类型 `SubjType` = 商户 / 链上地址 / 个人 / UBO,各带在案角色 / 金额 / KYC)。如扇入网络案件含 归集地址 + 3 商户 + 中转/出口地址(6 主体)、众包养卡团伙案件含 3 商户 + 设备群 + 个人/UBO。研判抽屉以主体列表展示(无 `subjects` 时回退为案由主体单行)。
- **双向并案(不止从事后)**:① **事后监控**端 —— 命中详情「一键并案」生成案件;② **案件管理**端 —— 整页工作台 Q4「**建议纳入**」自动列出同主体 / 同网络、其它维度、尚未纳入的命中,「**纳入**」一键并入(`caseStore.addSubject`,主体并集 + 记事件);两端都减少人工对齐。案对案的**合并**在第三层决策块的「合并 / 关联团伙」动作里(选目标案件)。
- **新增案件**(`components/NewCaseDrawer.tsx`):主体 / 类型 / 风险类型 / 优先级 / 金额 / 来源 / 关联,`caseStore.add` 建案进「调查中」。
- **优化(对比原 mockup)**:案件数据各自独立(非全同占位);**状态(生命周期)与 STR(报告产物)分列不重复**;补**来源**标记;认领/审核按分配门控;关联项 / STR 可跳转。

## 八、主体档案 · 360（`pages/EntityProfile.tsx`，`/entity`）
**跨模块商户360**。**主体 = 商户(法律实体)单一维度**(刻意决定):MSB 的 KYC 锚点、可问责对象(限额/冻结/STR/停业)都是商户;**地址 = 商户名下属性**(见下「关联链上地址」)、**行为 = 归属记录**,均不另作主体——避免同一坏人被摊成几十个地址档案。无归属的外部地址(混币器/制裁钱包/对手交易所)归 **名单管理** 或 **案件子主体**,不进主体档案。把商户在 **事中 · 事后 · 告警 · 团伙 · 案件 · 报送** 的全部足迹聚到一张视图,解决「同商户被各模块割裂、各开一案、重复 STR」。聚合逻辑 `lib/entity360.ts`(纯派生,只读静态数据;页面用各 store version hook 叠加实时状态)。
- **归一化匹配器**（核心）：同一主体在各模块名字写法不一（告警 `NovaPay Technologies Ltd.` / 事后·团伙 `NovaPay Technologies` / 报告 `OffshoreFX Ltd. · 0x7F4a…9c21`）。`entityKeys(name)` 把名字拆成可匹配「身份键」数组：**链上地址**（`0x…` / base58 截断）→ 地址键；**团伙号** `RING-xxxx-xxx`、**设备群** `#A7` → 各自键；**商户** → 去标点 + 去公司后缀噪声词（ltd/inc/corp/technologies/exchange/systems…）取核心 1-2 词。`sameEntity(a,b)` = 两名字任一键重合。一个报告主体含「商户 + 地址」两键，故两边都能命中。
- **足迹聚合** `footprint(name)`：`alerts`（按 merchant）/ `findings`（按 subject）/ `rings`（任一成员匹配，带本主体在团伙中的角色）/ `cases`（subject 或 `subjects[]` 任一匹配）/ `reports`（按 subject）。`totalExposure` 粗解析各记录金额求和给量级感。
- **主体目录 · 商户风险总览**（`/entity` 无参,`Directory`,对标用户 mockup 重做）：`directory()` **动态派生非手工维护** —— 扫描 告警 / 事后 / 团伙成员（剔群组）/ 案件主体（**子主体只纳入 `type==="商户"`,链上地址 / 个人 / UBO 如 Tornado Cash 不作主体行**）/ 报告 的全部名字按键聚合,末尾 `filter(type==="商户")` 只保留商户主体。每个商户进一步**派生风险字段**:`risk`(取最高告警分 + 制裁/在办案件/团伙托底)、`status`(提现冻结 / 受限·团伙 / 待报送 / 调查中 / 待补材料 / 名单观察 / 正常 / 白名单,由 sanction/ring/reportOpen/caseActive/risk 推导)、`merchantNo`·`vol30`·`lastEvent`(无真实值时按 key 种子**确定性合成**,刷新稳定)、`str`(报送 + 在办案件数)。少量干净 / 白名单 / 观察商户由 `DIR_SEED` 补入,凑齐风险谱。按 risk 降序。
  - **两轴状态(关键口径)** —— 旧版把「有没有命中」反推成单列状态(沾制裁告警就标冻结、在团伙图里就标受限),把**分析发现**和**账户动作**混为一谈。重构为两轴,各从真实来源派生:
    - **账户状态**(`acct`,动作驱动 = 被采取了什么约束):**冻结 / 受限 / 正常 / 观察 / 白名单**,来自**名单库 `lists.ts`**(制裁名单→冻结、内部黑名单→受限、关注名单→观察、白名单→白名单,只认 `status==="active"`,按商户名归一化匹配);**确认制裁敞口**(命中制裁名单 / 案件 type 含「制裁」,`e.sanction`)→ 冻结,**覆盖过期白名单**(有在办制裁案件的商户不该因旧白名单条目显「白名单」)。`accountState(name)`。
    - **处置进展**(`flow`,工作流阶段):**直接复用真实案件状态机 `CSTATE`**(调查中 / STR草稿 / MLRO评估 / 待报送 / 已报送),案件前后再加 **待研判**(有告警未立案)/ **已结**(closed/merged/无)。**不再用 caseActive 布尔把 5 个状态压成「调查中」**(旧版会把 MLRO评估 / 已报送 错显为调查中)。一个商户可同时挂**多个在办案件**(如 RapidPay ∈ 扇入网络 + 养卡团伙):取**最推进**的案件作主显示(用户口径「显最新状态」),并加 **`·N案`** 角标(`activeCases`>1,紫)—— 多 case 本身就是「重复立案 / 可串并」信号,呼应 `关联案件·串并` 主线,不被悄悄吞掉。**`·N案` 可点 → 跳该商户 360(`&merge=1` 自动定位)→ 顶部「串并横幅」列出全部在办案件 + 「一键串并到 {最推进主案}」:被并案件 `caseStore.set(id,"merged")` 转已合并、统一并入主案(一次审核、一份 STR),关联案件区实时联动。「发现多案 → 一键串并 → 状态联动」在总览闭环。`mergeable`/`doMerge`/`CASE_ORDER`。**紧急待办**(`pending`)作角标:**待认领** / **SLA临期**(只 red 才算,amber 是常态、不标)。`FlowCell` + `CASE_RANK`。
    - 修正:报告里「OffshoreFX Ltd. · 0x7F4a…9c21」含地址被 `entityType` 误判为链上地址 → 取**最长商户写法变体**作展示名(`merchantForm`),避免制裁商户被整条过滤丢失。
  - **页面**:**4 个 KPI 卡**(总商户数 · 活跃/受限 / 高风险主体 风险分≥80 / 有在办案件 涉及N主体 / 当前受限·冻结)+ **状态分桶 tab**(全部 / 高风险 / 有在办案件 / 受限·冻结[账户] / 名单观察[账户],带计数,`inTab`)+ 搜索(名/商户号)+ **风险分 / 账户状态 / 注册地** 三个 Select 筛选 + **富表格**(商户头像[确定性配色+缩写]+名+商户号 / **风险分徽标+趋势** / **账户状态 Pill** / **处置进展**[阶段+待办角标+可并案] / **犯事记录 chips**[告警·案件·STR·团伙] / 30日交易额 / 最近事件·日期 / 查看档案→)+ 底部「犯事记录」说明。点行进 360 档案。
  - **适用规则视图(商户视角:这个商户实际跑哪些规则)** —— 360 档案新增**「适用规则」卡** + 主体档案里的**业务模式标签** chips,回答「不是所有商户都跑所有规则」:`applicableRules`(`entity360.ts`)按商户的 风险分 / 注册地 / 白名单 / KYB / **业务模式标签**(只给 OTC·交易所 / 加密 ATM / DeFi 这类**派生不出来**的打标签;风险/KYC/辖区/名单为派生、不打标签)匹配每条**已上线**规则的 `audience`,分三桶:**法定核心 · 恒跑**(`cat==="名单筛查"` 制裁/名单硬规则 + LVCTR,所有商户不可豁免)/ **定向命中 · 适用**(audience 空或匹配)/ **不适用 · 未定向到**(audience 设定但不匹配)。规则可点进详情。内置 `新商户首充` 已设 `audience:["新户 / KYB 未完成"]` 作演示:新户 NovaPay 落「定向命中」、老白名单商户(Kraken-U)落「不适用」。
    - **标签怎么来:模型自动识别 → 人工确认(不靠手填死表)** —— 标签有两类来源:**种子**(`merchantTags`,开户分类模拟)+ **自动识别**(`suggestTags`,从商户**跨模块足迹**的行为特征确定性推断业务模式:出现 混币器/跨链桥/隐私币 → 建议 `DeFi·跨链`;频繁小额拆分/多方归集 → 建议 `加密 ATM`;快进快出过水 / 多对手地址 → 建议 `OTC·交易所`,带 reason + 置信度)。360 主体档案里出「**模型识别 · 建议标签(待确认)**」块,每条可**确认**(`tagStore.confirm` → 升为生效标签,实时联动「适用规则」三桶重新归类)或**忽略**(`tagStore.dismiss`);已确认标签可 `×` 移除。`tagStore`(`store.ts`,内存按归一化键存,刷新重置;`useTagVersion` 响应式)。真实系统:种子换 KYB 开户分类源、`suggestTags` 换特征模型 + 落人工复核队列。
  - **分诊增强(产品判断:目录是分诊工具而非排名榜)** —— 不只回答「谁分最高」,而是「谁需要我、为什么、是否在恶化」:① **可解释风险分** —— 点风险分 Popover 展开「风险驱动因素」(制裁 / 在办案件 / 团伙 / 高分告警 / 事后命中 / STR在途,复用全站加权贡献语言);② **风险趋势 ▲▼** —— 对比 30 天前的变化(确定性合成,低风险主体波动小),让「中分但恶化」也能浮上来;③ **需关注分诊条** —— 顶部把待办聚成可点筛选(待认领案件 / STR待报送 / 案件SLA临期 / 告警待研判 / 风险恶化中,`pending.kind` 派生)+ 表格「待处理」列显主要待办徽标;④ **可并案提示** —— 同团伙含其它商户时打「可并案」角标(`ringMates`,呼应「并案而非重复立案」主线);⑤ **默认按需关注度排序** `need = 风险 + 待办紧迫加权 + max(0,趋势)`,而非纯风险降序。派生在 `entity360.directory()`(`RiskFactor`/`Pending`/`trend`/`need`),UI 在 `RiskCell`/`TrendTag`/`PendingBadge`/`TriageStrip`。
- **商户 → 关联链上地址**（`linkedAddresses`，回答「搜商户能否带出其地址」）：商户键 ≠ 地址键,故商户钱包不会被 `footprint` 直接命中。改从该商户**告警的 `sender`/`receiver` 反推** —— 标「商户托管」=**本方持有钱包**,另一端=**交易对手地址**(入金来源 / 出金去向),去重。360 档案右栏(仅商户)出「关联链上地址」卡,作商户**属性展示**(地址不另作主体、**不点进当主体**;高风险外部地址归名单 / 案件)。如 NovaPay → 托管 `0x91Ad…77F2` + 对手 `0x5078…Ec8c`。**关联主体卡只列其它商户**(同团伙 / 同案),地址不进。
- **360 档案**（`/entity?name=`,`Profile`,**对标用户 mockup 重做**):**页头 + 基本信息 / 活动日志 两 tab**。
  - **页头**:返回链接 + 名称 + **账户状态 pill(非正常时)+ 风险词 pill**(高/中/低,由 `dir.risk`)+ 副行「商户号 · 注册国家 · 入网时间」(`merchantNo` 复用目录派生,入网时间确定性合成)。
  - **基本信息 tab**:① **串并横幅**(条件,见上);② **6 个 KPI 卡**(累计涉及金额 / 累计告警 / 事后命中 / 立案调查 / STR·报送 / 关联团伙,`MiniKpi`);③ **主体档案卡**(头像 + 商户号 + 尽调档案按钮 + 2 列字段 `Field`:KYC准入评级 / 注册地 / 类型[**从名称派生法律形式 Ltd./Inc.… + MSB**]/ 账户年龄 / 制裁PEP / UBO / 30日交易额 / KYB)**+ 关联网络卡**(关联链上地址列表 + **关联主体**带关联强度[团伙置信度→强/中/弱]与**硬证据依据**[取本主体与对方在团伙图中那条共享边的真实 `note`,如「共享 2 设备指纹 + 出口 IP」],可点跳对方 360);④ **完整事件记录卡**:头部「共 N 条 · 按时间倒序」+ **筛选 chips**(全部 / 各模块带计数,`evFilter`)+ **表格**(类型 / 事件 / 记录·关联单号 / 时间 / **记录现状**)——`events` 把五模块记录拍平为统一事件流,事件描述 + 合成时间戳 + 关联记录现状 Pill,可点跳原记录。
  - **活动日志 tab**:**操作审计轨迹**(`auditLog`)—— 每条记录的生命周期基线事件 + 各 store `eventsOf` 的真实操作事件(认领 / 处置 / 串并…),按时间倒序,复用 `Timeline`;与「完整事件记录」(记录清单)分工互补。
  - **口径优化**:记录现状列(非事件本身状态,消歧)/ KYC准入评级(静态,区分页头动态风险分)/ 关联依据落到团伙共享边硬证据 / `related` 案件子主体只取 `type==="商户"`(剔 Tornado Cash)。取代旧版「左六卡片 + 右侧栏」与「单卡多模块 tab」两版。
- **入口**：侧栏「主体档案」（`Fingerprint`，监控运营组，处置记录下方）+ 顶栏搜索按钮 → 主体目录 + **告警详情**「商户/客户画像」卡头「主体360」+ **案件详情**多主体切换器选中主体页头「主体360」。React hooks 安全：`EntityProfile` 仅按 `name` 选 `<Profile>` / `<Directory>`（两独立组件，hooks 顺序稳定）。

## 九、名单管理（`pages/ListsPage.tsx`，`/lists`）
喂入**实时筛查**的名单库 + 名单项生命周期。数据 `lib/lists.ts`；状态 / 经手 / 事件 / 新建存 `lib/store.ts` 的 `listStore`（内存，镜像 `ruleStore`）。
- **四类名单**（`ListCat` / `LCAT`）：**制裁名单**(sanctions，OFAC SDN / UN / SEMA，硬拦截·法定，红)/ **内部黑名单**(block，确认欺诈/洗钱主体·地址，拦截，紫)/ **关注名单**(watch，升级监控·不拦，黄)/ **白名单**(allow，已核验·豁免规则，绿)，各带 lucide 图标。
- **名单项**（`ListEntry`）：值（主体名 / 链上地址 / 钱包 / 设备 / IP）+ `entryType`(商户/个人/链上地址/钱包/IP/设备，配色 `LENTRY_TYPE_TONE`)+ 类别 + 风险 + **来源**(制裁筛查 / 案件 / 团伙 / 事后监控 / 手动，`srcId`+`to` 可跳 `/case`·`/ring`·`/alert`)+ 添加人/时间 + 理由 + 适用范围 + **近30天命中拦截数** `hits30` + 到期 + 状态。12 条种子覆盖四类与多 entryType；真实关联 `CASE-20260318-018`(OffshoreFX 制裁)/ `RING-2026-031` / `PM-2026-020` + OFAC 地址 `0x7F4a…9c21`。
- **状态机**（`LStatus` + `LFLOW` 门控）：`待复核 →(复核生效)→ 生效中 →(到期/移除)→`；`已暂停`(可恢复)/ `已过期`(可续期)/ `已移除`(终态)。**制裁项通常直接 `生效中`**(法定即时)。动作含 复核生效 / 暂停 / 恢复 / 续期 / 移除 / **升级为制裁**(记审计事件)。
- **列表页**：名单概览条（四类计数 + 近30天累计拦截 + 待复核数）+ 状态 tiles + 类别 chips + 搜索 + 表（名单项·类型徽标 / 类别 / 来源link / 适用范围 / 近30天命中 / 状态 / 添加人 / 到期 / 操作）+ **「新增名单项」**按钮。行点击进整页详情。
- **名单详情整页**（`pages/ListDetail.tsx`，`/list-entry?id=`，镜像 `RuleDetail`）：页头（值 + 类型/类别/状态徽标 + 状态门控操作）+ 基本信息 / 活动日志 tab。基本信息：KPI 四宫格（近30天命中 / 状态 / 来源 / 适用范围）+ 名单项概要 + **命中历史表**(`HitRow`：日期/交易/动作/金额)+ **关联**(`MatchRow`：命中的告警/案件/团伙可跳)+ **操作区**(状态门控动作卡 → `listStore.set` + toast + 审计事件)。详情数据 `LDETAIL` + `detailOf` 兜底。
- **新增名单项抽屉**（`components/AddToListDrawer.tsx`，镜像 `NewRuleDrawer`）：值 / 类型 / 类别（带各类指引）/ 适用范围 / 理由(必填) / 到期(选填，制裁·黑名单提示长期有效)。提交：制裁→直接生效、其余→待复核，`listStore.add` + toast。支持 `preset` 预填（供其它模块「加入名单」直达）。
- **闭环定位**：名单 = 筛查规则的「数据侧」—— 案件 / 团伙 / 事后确认的可疑主体·地址沉淀为名单项，喂回事中实时筛查拦下一笔（与规则回填闭环互补：规则=逻辑侧，名单=数据侧）。

## 九·补、全局策略 · 系统兜底（`pages/StrategyPage.tsx`，`/strategy`）
风控的**安全网底座**:在逐条「监控规则」(显式拦截逻辑) 与「名单」(筛查数据) 之外,系统**始终生效的全局默认与兜底**——回答「没规则命中怎么办 / 规则·名单冲突听谁的 / 数据缺失或系统降级时如何保守处置」。三层配置 = 逻辑(规则) + 数据(名单) + 兜底(本页)。
- **三原则**:**默认从严 fail-safe**(无命中 / 评分缺失·超时 → 默认更保守档,不默认放行)/ **最严生效 most-restrictive**(多命中取最严动作)/ **法定不可豁免**(制裁·LVCTR·STR·留痕始终生效)。
- **① 决策基线·风险分处置矩阵**:无规则命中时按综合风险分分诊 —— ≥80 拦截+人工 / 60-79 放行+转研判 / 40-59 放行+留痕 / <40 直接放行;**评分缺失·超时默认按「偏高·转研判」**(从严)、大额暂缓。**呈现改版**:原三列表格换成 **0–100 渐变分段条**(四档按区间跨度着色 `<40` 占 40%、其余各 20%,低→高 左→右,下方 0/40/60/80/100 刻度)+ 下方四张**档位卡**(区间 / 动作 / 说明,卡头一颗小色点标严重度,正文中性)。**借鉴 Stripe Radar「Risk controls」**:段条上方加一张**「当前处置基线」突出卡**(盾/表盘图标 + 当前预设档徽标 + 一句话当前阈值「≥{block} 拦截+强制人工 · {review}–{block-1} 转研判 · <{log} 放行」+ 右侧「调整基线」按钮,呼应 Stripe「Currently blocking payments with a risk score of 75 and higher / Select risk setting」)。
  - **调整基线操作(真交互,非占位)**:处置基线 = 三个风险分切点 `Thresholds{block/review/log}`(默认 80/60/40),四档区间由 `makeBands` 派生、段条宽度按切点跨度算。点「调整基线」开 **预设档弹窗**(对标 Stripe「Select risk setting」):三档 `BASELINE_PRESETS` —— **保守·更早拦截**(70/50/30)/ **标准·平衡(推荐)**(80/60/40)/ **宽松·减少打扰**(90/70/50),每档卡内单选 + 切点预览 chips(≥block 拦截 / 转研判 / 留痕 / 放行)。选档「提交双人复核」即 `setThresh` —— **段条 / 档位卡 / 当前基线卡文案实时联动**(本地 state 演示,toast 提示已提交双人复核 + 记审计;接后端落 `policyStore`)。未改档时提交按钮禁用。法定硬约束(制裁 / LVCTR / STR)不受基线影响,弹窗内注明。
- **② 法定硬约束(锁定·只读)**:制裁筛查(命中即冻结+TPR,白名单不可豁免)/ LVCTR ≥CAD 10,000 自动报送 / STR 30 日时限 / 记录保留 ≥5 年。
- **③ 冲突仲裁·优先级**:最严生效;名单 制裁>黑>关注>白(白名单仅豁免评分类、不豁免制裁/黑);层级 法定>名单>模型>规则>基线。
- **④ 准入与默认限额**:未完成 KYB 默认禁提现+限额 / 新商户首充观察期 / 按 KYC 分级三档限额 / 高风险辖区加严 EDD。
- **⑤ 数据缺失·系统降级兜底(核心)**:链上溯源失败→转人工暂缓 / 制裁名单源不可达→fail-closed 暂停放行(锁定) / 评分服务故障→**熔断·全局保守模式** / KYC 缺失→按最高风险档 / 通道拥塞→低额优先余者排队。
- **⑥ SLA 超时兜底**:告警超时→自动暂缓冻结(防超时即放行) / 案件超时→升级 MLRO / STR 临期<5天→加急 / 名单到期未复核→维持生效(从严)。
- **⑦ 变更治理**:可调项双人复核(MLRO+风控负责人)+ 全量审计 + 灰度回测;法定项只读。
- **实现**:可调项为本地 `Switch` 演示态(toast + 审计提示,接后端落 `policyStore` + 双人复核);`Section`/`Policy` 复用,锁定项恒只读。路由 `/strategy`(原导航死链,本次补上)。
- **`Policy` 行 = Stripe Radar 式控件行**:左 **图标方块**(每条策略一个 lucide 图标,圆角灰底)+ 中 名称/取值/说明 + 右 **状态徽标**。三种性质由右侧状态徽标区分(不再用左色条):**法定锁定**→「法定 · 只读」封条 / **可调兜底**→「生效中(绿)· 已关闭(灰)」+ 开关 / **固定基线**→「默认生效」。`icon` 为必传 prop。
- **配色克制(关键约束:只有「状态」可用色)**:取值徽标(如「≥CAD 10,000」「最严生效」「全局保守模式」)一律**中性灰** —— 取值是「设置值」不是状态,不上色;**唯一可用色处** = 右侧状态徽标(生效中=绿)、风险分严重度(渐变段条 + 档位卡的小色点)、当前姿态条的健康绿点。原先值徽标按 red/amber/blue/violet/green 上色导致「花花绿绿」,本次全部收敛为灰,`Policy` 去掉 `valueTone` prop。

## 九·补²、审计日志 · 全量留痕（`pages/AuditLog.tsx`，`/audit`）
风控**全模块的统一不可篡改操作轨迹** —— 回答「谁在何时对哪条记录做了什么、为什么」。原导航死链(STUB),本次补上整页。供 MLRO 与监管追溯,呼应全站「全量留痕 / 记入审计日志」的合规承诺。数据 `lib/audit.ts`。
- **数据 = 静态种子 + 本会话实时事件**(与各 store 同源):各 store 新增 **`allEvents()`**(`store.ts`,翻平内部 `data` map 为 `{id,t,text,reason}[]`),`liveAudit()` 聚合 规则/案件/报送/名单/团伙/事后/告警 七个 store 的实时事件 → 审计条目(模块按 store 定,操作人按模块默认:治理类→总管/MLRO、运营类→L1;类别 `catOf` 按动作文本推断);**`AUDIT_SEED`** 24 条代表性历史轨迹(跨模块/角色/类别,绑真实记录 id → 深链可跳),最新在前。`allAudit()` = 实时(标「本会话」)在前 + 种子。刷新重置(内存态)。
- **`AuditEntry`**:`date/time/live? · actor(Person)+role · module(9 类)· cat(审批/处置/创建变更/状态流转/访问导出)· action · target(+targetId 深链)· reason`。**9 模块**(`AUDIT_MOD`,各带 lucide 图标 + 深链构造):规则 `/rule`、案件 `/case`、报送 `/report`、名单 `/list-entry`、团伙 `/ring`、事后 `/finding`、告警 `/alert`、策略 `/strategy`、系统(无链)。**高敏动作**(`isHiSensitive`:冻结/上线/签发/报送/作废/制裁/停用/基线/并案/升级MLRO)列表打中性 `ShieldAlert` 标记。
- **页面**:4 KPI(操作记录 / 决策类[审批+处置] / 涉及操作人 / 高敏动作)+ **模块筛选 chips**(全部 + 9 模块带图标)+ **类别 / 操作人 Select** + 搜索 + **只读轨迹表**(时间[本会话标记] / 操作人[头像+角色] / 模块 / 对象[可跳] / 动作[高敏标] / 理由)。订阅七个 `useXVersion` —— 本会话任意模块的认领/处置/审批/报送等动作即时汇入。
- **配色 / 去 emoji 口径一致**:模块徽标=属性中性灰;操作人头像保留身份色(JL 蓝 / EZ·SC 紫 / RH 青 / 系统灰);高敏标记中性灰;链接品牌色。无 emoji(系统操作人初始用「系」)。
- **路由**:`App.tsx` 去 STUB,`lazy(AuditLog)` + `/audit` 路由;侧栏「审计日志」(`Clock` 图标,治理与合规组末项)。

## 十、团伙簇节点展开（`kind:"群组"` 子成员,已完成）
团伙图谱里 `kind:"群组"` 的簇节点（中转钱包 ×3 / 拆分群组 #B1 / 收款地址 ×4 / 设备群 DV-7 / 新注册商户 ×2…）原本只是折叠占位、无下钻数据。现可**点击展开看子成员**：
- **数据**（`lib/rings.ts`）：`ClusterChild { v; meta; tone }` + `RingMember.children?`。关键团伙手写子成员（021 关注名单 #A7 的 5 地址、055 制裁规避中转钱包触及 OFAC），其余由 **`clusterChildren(m)` 确定性合成** —— `clusterCount` 从 `×N` / 「N 地址/个/设备」解析数量，`clusterKind` 按 名称/角色 判 钱包/地址/设备/商户，种子取自父节点名（FNV-1a + LCG，刷新稳定不随机），按类型产出 `0x…` 截断地址 / `DV-xxxx` 设备指纹 / 新注册商户，名单类标 amber，制裁类标 red。
- **交互**（`pages/RingDetail.tsx`）：① **图谱节点** —— 簇节点带**数量角标 + 展开箭头**、可点击（高亮描边），点击滚动到对应成员行；② **团伙成员卡** —— 簇行整行（跨两列）带「展开/收起」，内联子成员清单（风险点 + 等宽地址值 + meta）。两处共享 `expClusters` 状态。

## 十一、待办 / 可继续
- ~~**告警研判「转合规」动作**~~：**已作废** —— 后续架构确立「STR 唯一发起点 = 案件管理」（见第五、七节），告警可疑→走「建案」、案件内起草 STR，不再有「告警转合规直连 STR」（避免重复立案 / 重复 STR）。
- **持久化**：`alertStore` / `ringStore` 为内存，刷新重置；接后端需替换。
- **事后监控转研判**目前是本地标记（未真正生成 alert 记录，因 `alerts` 为静态数组）；接后端后应真正创建告警。
- 仪表盘大量数字为 mock；事中监控「实时刷新」为文案占位（无真实轮询）。
- SLA 起算时点（识别 vs 认领 / 实时命中）尚未严格定义。

## 十二、全站巡检 · 一致性修复（2026-06-30）
四路并行巡检(导航链路 / 配色口径 / 死代码 / 跨模块数据一致性)后,修以下确认项(导航无死链、状态机门控均为设计如此非 bug):
- **死代码** —— `App.tsx` 删 `STUBS=["/strategy"]` + Placeholder 路由 + lazy import(/strategy 早有真路由,stub 永不命中);删除 `pages/Placeholder.tsx`。
- **配色收敛补漏**(承接「只有状态用色」战役遗漏的 3 处属性徽标 → 中性灰):`AlertDetail` 商户**分级**(原按 tier 上 amber)、`ReportDetail` 涉事主体**类型**(实体/个人,原 blue/amber,与全站 `SUBJ_TONE→灰` 同口径)、`ListDetail` KPI「来源」/「适用范围」(原 blue/violet)。`ListDetail:159` 命中动作配色**保留**(doc 既定「结果/命中动作单元格可保留色」)。
- **真·显示缺口** —— `cases.ts` `strLabel()` 补 `merged` 分支(原 fallback 显「—」)→「并入主案」(已合并案件本身无独立 STR,由主案统一起草)。
- **文案间距统一** —— `findings.ts` `FSTATES` 的「已结·X / 已出账·追溯中」改为带空格「已结 · X」,对齐 `data.ts` `RC_STATES` 三个 closed_ 标签与 findings 自身 `profile.history` 串的主流「 · 」写法。

## 十三、顶栏通知中心 + 操作员菜单(`components/Shell.tsx`,2026-06-30)
顶栏铃铛原是**死按钮**(常亮红点、点击无反应),操作员胶囊(风控·L1)有下拉箭头也无菜单。本次做成真交互:
- **通知中心(铃铛)** —— 数据源 `lib/notifications.ts` 的 `liveNotifications()`:团队级「需立即处理」信号**全部从 store 实时派生**,**口径与仪表盘「需立即处理」banner 一致**(同名谓词:告警超SLA / FINTRAC报送待处理 / 案件待认领 / 待认领团伙),零计数项自动隐去。铃铛**红点改为计数徽标**(`notifTotal`,>9 显「9+」;清空时无徽标);点开 HeroUI `Popover` 面板:每项 = 图标 + 标题(带数)+ 副说明 + `ChevronRight`,**点击深链到对应模块**(`/alerts`·`/reports`·`/cases`·`/rings`)并自动收起;空态显绿 ✓「已全部清空」;底部「查看运营总览 →」跳 `/ops`。Shell 订阅 `useAlertVersion/useCaseVersion/useReportVersion/useRingVersion` —— 本会话任意认领/处置/报送动作即时反映到通知数(全站任一页都在线)。
- **操作员菜单(胶囊)** —— `Popover`:身份卡(当前 `who` 头像 + 姓名 + 角色 · 层级)+ **切换身份 · 演示**(分析师 James Liu ↔ 风控总管 Emma Zhang,当前项打 ✓)+「主体档案 360」(→`/entity`)+「退出登录」(演示态 →`/dashboard`)。原独立头像并入触发器。
- **配色口径** —— 计数徽标用 danger(真·紧急状态)、空态 ✓ 用 success、链接用 brand;面板图标方块/副文中性灰,符合「只有状态用色」。
- **role-aware + 全局操作员身份(`roleStore`)** —— 新增 `store.ts` 的 `roleStore`(`Role` = analyst/head、`PERSONS`、`ROLE_META`、`useRoleVersion`,`useSyncExternalStore` 同款,内存刷新重置),**顶栏与仪表盘视角共享同一身份**:① 顶栏胶囊「切换身份」即时改全局 role → 铃铛通知集随之切(`liveNotifications(role)`:分析师=团队态势全量 4 项;**总管=只留团队 SLA 告急**,报送已在「待我审批」、待认领已并入「团队负荷」,与仪表盘 banner 同口径不重复)+ 头像/姓名/`chip` 文案切换;② 仪表盘原**本地 `useState` 角色切换改读 `roleStore`**(`PERSONS.analyst/head` 取代本地 ME/HEAD 字面量,pill `onClick → roleStore.set`),故顶栏切身份 ↔ 仪表盘视角双向联动、全站一处来源。

## 十四、端到端走通审计 + 补死路(2026-06-30)
逐链路核对一笔可疑交易能否从检测一路走到结案,确认主链无硬死路、补两处软死路 / 错路。
- **审计结论(主链结构完整,无硬死路)** —— ① 跨模块**建案收口**:事中驳回/告警升级(`ReviewDialog` 选 建案/拒绝)、事后转案件(`FindingReviewDialog`)、团伙转案件(`/ring?id=&action=case`)四个入口全部走 `intakeCase`(同主体在办则并入、否则 `caseStore.add` 真新建);② **案件 → 报送**:STR 不显式创建,而由 `reportsAll.caseDerivedReports()` 从 STR 态(`str_draft/mlro/queued/filed`)案件**派生**为报告(`STR-{caseId}`,制裁规避走 TPR 不派生),且派生报告在报告报送**可被操作**(`ReportDetail` 动作写 `reportStore.set(STR-…)` 覆盖);③ 案件状态机(`investigating→str_draft→mlro→queued→filed→closed`)与报告状态机(`draft→review→queued→filed→ack`)均可达终态,`CaseReviewDrawer` 恒提供「结案归档」。
- **补①:结案闭环可一键触发** —— 关联 STR 报送/回执后,`CaseDetail` 联动横幅原只提示「本案可结案归档」却仅有「查看报送」链接(用户须自行翻 审核→案件管理→结案归档)。现横幅在 `repSt∈{filed,ack}` 且案件已认领时直出**「结案归档」主按钮** → 开 `CaseReviewDrawer` 并 `preselect="archive"`,端到端最后一步就地收尾。
- **补②:案件审核动作按状态门控(去回退/自环错路)** —— `CaseReviewDrawer` 原**无差别**渲染全部 `CASE_OP_GROUPS`,故 `filed`/`mlro` 态仍显「起草STR」(回退 str_draft)、「转MLRO」(自环)等死路。新增 `cases.caseOpAllowed(op,st)` + `caseOpGroupsFor(st)`:用既有 `investigating→…→filed` 时序,**只放行前进动作 + 终态处置(结案/合并)+ 过程动作(请求信息/升级/关联)**,排除回退与自环;**误报放行**(`fp`)限 `≤mlro` 态(STR 入队/已报送后不可误报放行,改走作废/归档)。空分组自动隐去。

## 十五、团伙关系图谱布局重做 —— force-directed → 分层(2026-06-30)
6+ 成员团伙原用**手写 force-directed(Fruchterman–Reingold)**布局(`RingDetail.forceLayout`),初始温度 0.9 在单位空间过热、加上重力,导致**节点 / 标签重叠、连线打结、跑出框** —— 用户反馈「图谱布局乱」。换成**确定性分层布局**(`layeredLayout`):
- **算法**:取**度数最高的节点为枢纽**(hub,如 082 的 GlobalRemit 度 5 / 070 的 RapidPay 度 3),从 hub 做 **BFS**,按图距分**列**(L0=hub、L1=直接邻居…)。BFS 性质保证**每条边只跨相邻列**(或同列内短连),故连线天然不交叉、资金流向左→右读。**按无向图处理**(数据里 `edge.a→b` 方向不一致:070 首条边存反了,不能依赖方向)。
- **无重叠**:同列节点按 `rowGap = size+48` 垂直均布、列距 `colGap = innerW/(cols-1)`;大团伙画布加宽到 `W=640`,实测 082(12 主体)colGap 131 > 标签宽 88、070(7 主体)125 > 109,横竖都不挤。画布高 `H` 随最宽列的节点数自适应。
- **范围**:`n≥6` 走分层(原 `force` 分支),`n≤5` 仍走干净圆环(未动)。`forceLayout` 整段删除。
- 结果:082 列为「GlobalRemit → 拆分源×3 / 归集 / 关联商户 → 设备群 / 中转×2 → 出口×2 → HavenPay」,070 为枢纽 + 链,均为清晰的左→右流向图,与案件资金链路图(分层有向)观感一致。

## 十六、风控事中决策引擎 + 测试(2026-07-01)
原型此前只有 UI + 元数据,无可执行决策逻辑。新增**纯函数决策引擎** `lib/decision.ts`(无 mock / 无 TODO),把 `/strategy` 文档的政策落成可测代码,并配 vitest + 覆盖率。
- **引擎管线**(`decide(txn, health)`):① 输入合法性(金额非有限/负 → fail-safe 暂缓)② 制裁筛查(命中→冻结+TPR,白名单不豁免)③ 名单筛查(制裁>黑>关注>白,**仅 active 生效**;白名单仅豁免评分类)④ LVCTR ≥CAD 10,000 自动报送 ⑤ 加权规则评分 CRR ⑥ 基线矩阵(拦截 80/研判 60/留痕 40)⑦ **最严生效** + 降级兜底。输出 `{action,score,band,reports,reasons,degraded,manualReview}`。
- **同源**:8 条评分规则(`SCORING_RULES`)的 id/权重/条件取自 `rules.ts` 已上线 gate 规则(R-AMT-001/002·R-CHN-001/002·R-SCR-001·R-BHV-001/002/003);名单 `screenValue` 直接读 `lists.ts`(active),用真实制裁地址/商户/白名单种子。
- **降级策略**:制裁源不可达→fail-closed 暂停放行;评分超时/故障→熔断保守(默认转研判,大额暂缓,依赖评分的规则跳过不当未命中);链上溯源失败→链上规则转人工暂缓;KYC/KYB 缺失→最高风险档。
- **测试**(`decision.test.ts` + `decision.regression.test.ts`,`npm run test` / `test:cov`):每条规则 命中/不命中/边界;7 项绕过尝试(拆单规避 LVCTR、9999.99 边界试探、负数/NaN/Infinity 字段异常、制裁名大小写空格规避、白名单滥用、pending 名单未生效)均验证拦截;4 项降级;19 条回归样本(期望取自政策口径,非引擎自证)对照表全 PASS。**44/44 通过,覆盖率 100% stmt/func/line、98% branch**(2 个未覆盖为防御性死分支:`!value` 守卫被 `screenTxn` 预过滤、rank 升级分支因种子排序制裁在前不可达)。
- **范围/限制**:单笔决策(滑窗特征靠上游预聚合喂入,非引擎内多笔状态);名单匹配为大小写/空格归一 + 子串松匹配(**未防 Unicode 同形字/音近变体**);阈值取默认基线(未接 `policyStore` 动态档);速度/集中度等事后/batch 规则不在事中引擎内。

## 十七、调查工作台 · 数据契约层(Phase 0+1,按 IMPLEMENTATION.md;2026-07-01)
按外部规格 `IMPLEMENTATION.md` 在 **rc-app 内**搭「主体档案 / 关联团伙 / 团伙详情」调查端,**全量数据层**(Zod 契约 + MSW mock + TanStack Query),UI 用 **HeroUI**(视觉基准=rc-app 现有页面)。本阶段只到数据层,页面在后续 Phase 3–6。
- **Phase 0 脚手架**:`zod` + `@tanstack/react-query` + `msw` 接入;`main.tsx` 启动 MSW worker(本原型无后端,**MSW 即后端**,dev/线上都靠它拦 `/api`)后再渲染,外裹 `QueryClientProvider`;`msw init public/` 生成 worker。
- **Phase 1 数据契约**(单一来源 = Zod):`schemas/{common,subject,ring}.ts` 定义 DimensionKey/AssocEvidence/Subject/MergedAccount/MergeCandidate/SubjectDetail/Ring/RingMember/RingEdge/RingNode/FundFlow/RingDetail —— **刻意不含账本(MSO/MSB)字段**(§7.7)。`mocks/fixtures/`:主体 12 / 团伙 8 / 候选 6(ID 唯一,候选 confidence 全落 50–74 复核区间);详情由 `buildSubjectDetail`/`buildRingDetail` 确定性派生(seed=id,**用无符号位移 `>>>` 避免负索引**)。`mocks/handlers.ts`:全部 `/api` 路由,列表分页/筛选/搜索基于 fixtures 真实实现 + merge/split/claim/reject 改内存态(`*` 通配前缀以兼容浏览器同源与 node 测试绝对 URL);`lib/{api,queryClient,format,domain}.ts`(domain = §7 阈值 75/50/74、认领状态机、合并/拆分所需角色);`hooks/{useSubjects,useRings}.ts`。
- **领域常量**(`lib/domain.ts`):归并 自动 ≥75 / 复核 50–74 / <50 不并;认领仅 `pending` 且无 assignee;合并需「风控分析师」、拆分需「风控主管」(前端不鉴权,但 UI 后续须呈现权限 + 审计提示)。
- **门禁**(spec 第九节,npm 等价):`typecheck` ✓(tsc strict 0 error) · `test` ✓(**61/61**,`mocks/contract.test.ts` 校验全部 fixtures + 派生详情过 Zod、handler 真实过滤/分页/认领状态机/merge·split 改态) · `build` ✓。`lint`:**新增数据层文件全 clean**;`eslint .` 另报 rc-app **既有**文件(RingDetail/CaseDetail/Dashboard/bits/ui 等)的 react-hooks v7 严格规则(set-state-in-effect 等)历史欠账,**非本阶段引入**,留作单独清理(见待办)。

## 十八、调查工作台 · 主体归并工作台(Phase 3 首个 UI 页,`pages/SubjectWorkbench.tsx`,`/subjects`;2026-07-01)
§17 的数据层此前**零 UI 消费**(仅接 mocks/tests)。本阶段落**首个消费页**,端到端点亮整条链路(TanStack Query → MSW → Zod 校验 → HeroUI 渲染 + 变更),视觉基准=rc-app 现有页面(`Shell`/`PageHead`/`Pill`/`Initials`/`card` 复用,只有「状态」用色)。选「主体归并」作 Phase 3 首页,因它是新数据层**独有、现有页面无对应**的能力,且唯一能演练 merge/split/reject 变更 + §7 角色·审计门。
- **① 归并候选复核队列**(`useMergeCandidates`,50–74 复核区间):每张候选卡 = `账户ID —GitMerge→ 目标主体`(可跳 `/entity`)+ 检出日 + **置信度条**(复核区间统一 amber)+ **关联证据维度片**(按 `DIM_ORDER` 取命中维,弱信号降透明度,附 note)+ **归并 / 驳回** 动作。空态显绿 ✓「复核队列已清空」,加载显 `Spinner`;有候选时顶部 `Pill` 报「N 项待复核」。
- **变更 + 确认弹窗**(真交互):归并 `useMergeAccount({id:目标主体, accountId, reason})`、驳回 `useRejectCandidate({candidateId, reason})`。点动作开 `Modal`:摘要(归并=`账户→主体`+置信度 / 驳回=保留独立主体)+ **高敏封条**(「需 `REQUIRED_ROLE.merge`=风控分析师 权限 · 提交后全量记入审计(原型不实际鉴权)」,呼应 §7.5)+ **理由必填**(空则提交禁用)。成功 `toast` + Query `invalidate`(候选/主体列表自动刷新,MSW 内存态已真实改)。
- **② 主体档案列表**(`useSubjects`,MSW handler 真实实现):搜索(名称/ID)+ **风险等级** / **KYC 状态** 双 `Select` 筛选 + **分页**(每页 10,`total>10` 显上/下页 + 页码);筛选/搜索变更回第 1 页。行:主体(灰 `Initials`+ 名 + ID·实体/个人)/ 风险(`LEVEL_TONE` 高红中黄低绿 + 分)/ 归并账户数 / 涉及金额(`fmtCadCompact`)/ AML 命中(+ 规则命中)/ KYC(`KYC_META`)/ 关联团伙(可跳 `/ring`)。行点击跳 `/entity?id=`。`isLoading` 显骨架文案、`isError` 显「加载失败」。
- **路由 / 导航**:`App.tsx` `lazy(SubjectWorkbench)` + `/subjects`;侧栏「检测策略」组 团伙识别 下加「主体归并」(`GitMerge` 图标)。
- **门禁**:`typecheck` ✓ · `test` ✓ 61/61(未回归)· `build` ✓ · 新页 `eslint` clean。
- **接续**(Phase 4–6 待做):主体详情页(`useSubject` → `SubjectDetail`:归并账户表 + 拆分 `useSplitAccount` + mergeLog + amlHits)现仍跳旧 `/entity`(静态数据),后续应换新数据层;团伙列表/详情(`useRings`/`useRing`)同样待接页(现走旧 `lib/rings.ts`)。

## 十九、调查工作台 · 主体详情页(Phase 4,`pages/SubjectDetail.tsx`,`/subject?id=`;2026-07-01)
承 §18 接续。落**新数据层的第二个消费页 + 唯一的读写详情页**:`useSubject(id)` → `SubjectDetail`(整个契约里字段最全的类型,§17 定义)。§18 工作台的行点击与候选卡目标链接由旧 `/entity`(静态)**改指本页**,新数据层至此可端到端点亮到详情层。视觉基准仍=现有页面(`Shell`/`PageHead`/`card`/`Pill`/`Initials`/`SectionLabel` 复用,只有「状态」用色)。
- **概览**:身份卡(大 `Initials` + 风险 `Pill`(`LEVEL_TONE`)+ KYC `Pill`(`KYC_META`)+ 关联团伙徽标,可跳 `/ring`)+ 5 张统计卡(归并置信度 / 归并账户 / 涉及金额 `fmtCadCompact`+全额 / AML 命中 + 规则命中 / 账龄 `fmtAge`)。
- **① 归并账户表**(`accounts: MergedAccount[]`):账户 ID(mono)+ 锚点标记(`Anchor`,tooltip「不可拆分」)/ 归并依据 `meta` / **关联强度条**(非状态量 → **中性灰**,锚点为 `null` 显「—」)/ 注册日。**拆分动作**仅对非锚点账户可用 → `useSplitAccount({id, accountId, reason})`,开确认 `Modal`:摘要「拆出 → 恢复独立主体 + 触发风险重算」+ **§7.5 高敏封条**(需 `REQUIRED_ROLE.split`=**风控主管** · 全量记入审计)+ **理由必填**;成功 `toast`(含「触发风险重算」,呼应 handler `recalcTriggered`)+ `invalidate`(`subject`/`subjects`)。
- **② 归并依据**(`mergeEvidence: AssocEvidence[]`):按 `DIM_ORDER` 取命中维,每行=维度色圆点(`DIM_META`)+ 维度名 + 强/弱信号 + 权重 + note;弱信号降透明度(与工作台维度片同一视觉语言)。
- **③ AML 命中**(`amlHits`):卡列表(标题 + detail + 检出日),空态绿 ✓「无 AML 命中」。**④ 待复核候选**(`pendingCandidates`):仅当 >0 显,列账户 + 置信度 + 「去复核队列处理」跳 `/subjects`(复核动作集中在工作台,详情页不重复变更入口)。**⑤ 归并日志**(`mergeLog`):时间线,`auto_merge`/`manual_merge`/`manual_split` 各自图标 + 操作者 + 时间 + 理由(有则显)。
- **状态**:`isLoading` 居中 `Spinner`;`isError`/无 `id`/404 → 空态 + 「返回工作台」;`useSubject` 的 `enabled: !!id` 避免无 ID 空查。
- **路由**:`App.tsx` `lazy(SubjectDetail)` + `/subject`。
- **门禁**:`typecheck` ✓ · `test` ✓ 61/61(未回归)· `build` ✓ · 新页 `eslint` clean。
- **接续**(Phase 5–6 待做):团伙列表 / 详情(`useRings`/`useRing` → `Ring`/`RingDetail`)仍走旧 `lib/rings.ts` 静态页,待接新数据层。

## 二十、领域模型修正 · 实体层级改为「钱包地址 → 商户/主体 → 团伙」(2026-07-01)
与业务对齐后修正一个**建模口径错误**:此前把主体下的原子单位建成「账户 `ACCT-xxx`」,并按「一个人多个账户」讲归并。真实模型是——

```
钱包地址(多)──▶ 商户 = 公司主体(1:1)──▶ 团伙(同一控制人的多个商户)
  wallet            merchant / subject          ring
```

- **公司主体 ↔ 商户 是 1:1**:一个公司在系统里开一个商户,商户即该公司主体的载体;不存在「一个公司名下多账户待归并」。
- **一个商户下可挂多个钱包地址**:碎片化发生在**钱包地址**层。
- 因此「**归并**」的真实语义修正为**钱包地址归属识别**:一个(链上观察到的)钱包地址应归属到哪个商户/主体。明确登记的直接归属;靠关联信号推断的落置信度——≥75 自动归属、50–74 进人工复核队列、<50 不归属。**拆分**=把误归属的钱包地址从主体**剔除**并触发风险重算。
- **落地改动**(数据层单一来源 = Zod,改契约即全链路生效):
  - `schemas/subject.ts`:`MergedAccount`→`MergedWallet`(`accountId`→`address`);`Subject.accountCount`→`walletCount`;`MergeCandidate.accountId`→`address`;`MergeLogEntry.account`→`wallet`;`SubjectDetail.accounts`→`wallets`。
  - `schemas/ring.ts`:`Ring.accountCount`/`RingMember.accountCount`→`walletCount`(涉及钱包地址数)。
  - `fixtures`:钱包地址由 `walletOf` 确定性生成(`0x…` 截断形式);候选/handler(merge/split 改 `walletCount`、按 `address` 消费候选)/hook(mutation 传 `address`)/`contract.test` 同步。
  - 两页文案:「归并账户」→「归属钱包地址」、「账户」→「钱包地址」、`type:entity`「实体」→「**商户**」/ `individual`「个人」→「**自然人**」;动作动词 归并→**归属**、拆分=**剔除归属**;详情页锚点「锚点账户」→「**登记地址**(商户注册主地址,不可拆分)」。
- **门禁**:`typecheck` ✓ · `test` ✓ 61/61(未回归)· `build` ✓ · 全部触及文件 `eslint` clean。

## 二十一、领域模型再修正 · 提现白名单 → 砍「主体归并」保留团伙,商户档案化(2026-07-01)
再次与业务对齐,推翻 §20 里「钱包归属需人工复核」的设想。真实约束:**商户侧提现必须先把地址加入提现白名单**——即 商户 → 地址 的绑定是**已申报、行政确定**的事实,`归属` 不是一个风控决策(只是读白名单)。因此:

- **砍掉「主体归并 = 钱包归属」工作台**:候选复核队列 / 归并 / 拆分 / 归并日志全部移除(那套建立在错误前提上)。
- **保留并重定位「团伙」为核心线**。碎片化与研判发生在**商户之间**:同一实际控制人开多个商户,**共用同一提现白名单地址 / 设备 / 资金归集** → 这才是需要人工研判的团伙信号。白名单不但没挡住,反而**制造了关联证据**。
- **① 商户档案(`/subjects`、`/subject`,原主体归并页重定位为只读档案)**:
  - `SubjectWorkbench` → **商户档案列表**:去候选队列 + `useMergeCandidates/useMergeAccount/useRejectCandidate`,只保留 `useSubjects` 检索(搜索 + 风险/KYC 筛选 + 分页),行跳只读档案 `/subject`。
  - `SubjectDetail` → **只读商户档案**:概览 + **提现白名单地址表**(去「关联强度 / 拆分」,标 主结算/提现地址 + 登记日)+ **所属团伙卡**(跳 `/ring`)+ AML 命中。删拆分 `useSplitAccount`、待复核候选、归并日志。
  - 侧栏「主体归并」(`GitMerge`)→「**商户档案**」(`Store`)。数据层 hooks(merge/split/reject)保留备用,当前无 UI 消费。
- **② 团伙页按修正模型增强(路径 = 增强现有成熟页,非接新数据层)**:探查结论显示旧 `/ring`(485 行,静态 `lib/rings.ts` + `ringStore`)已具备关系图谱 / 逐边证据 / 置信度构成 / **研判处置抽屉(转案件 `intakeCase` + 状态机门控 + 每处置动态表单)**,而新数据层 `useRings/useRing` 反更薄(缺处置/转案件/簇展开等 7 处契约)。为不丢成熟工作流,选择**增强旧页**而非接新数据层:
  - `DIM_META.address` 维度改称「**提现白名单地址**」(全页 vocab 一次到位:图例 / 逐边证据 / 置信度构成)。
  - 新增 **核心关联证据 banner**(`CoreEvidence`,团伙详情置顶):突出「N 个商户经 M 个共用提现白名单地址关联归集」+ 列 address 维度逐边说明;无强信号团伙降级提示「关联偏弱」。
  - **成员 → 商户档案下钻**:`subjectIdForMember`(按名称映射商户成员→`SUBJ-` id)让商户成员卡可跳 `/subject`,契合「成员=商户主体」修正模型;非建档成员(名单群组/纯地址)无下钻。
- **门禁**:`typecheck` ✓ · `test` ✓ 61/61(未回归)· `build` ✓ · 新增代码 `eslint` clean(`RingDetail` deep-link `useEffect` 的 react-hooks v7 报错为改动前既有历史欠账,留单独清理)。

## 二十二、导航去重 · 合并「商户档案」入 已有「主体档案 /entity」(2026-07-01)
§21 把主体页重定位成「商户档案」后,与已有的「主体档案」(`/entity` · `EntityProfile`,商户总览 + 360° 档案)**职责重复**,侧栏出现两个档案入口。按「能合并就合并,不堆菜单」原则收敛:

- **保留成熟且已打通的 `/entity`**:它更全(适用规则 / 商户标签 / 足迹 / 敞口 / 在办案件串并 / linkedAddresses),且**告警详情、案件详情、顶栏搜索**均已链到它(按 `?name=` 键,Profile 按名确定性合成,可承接任意商户)。
- **删除新增的 `/subjects`、`/subject` 两页**(`SubjectWorkbench`/`SubjectDetail`)+ 路由 + 侧栏「商户档案」入口(`Store`)。
- **团伙成员下钻改指 `/entity?name=`**:`RingDetail` 商户成员(`m.kind==="商户"`)统一跳主体档案 360(不再限于映射表,任意商户成员可跳);移除本会话临时加的 `subjectIdForMember`/名称映射。
- **遗留**:「调查工作台」新数据层(`schemas/{subject,ring}`、`hooks/{useSubjects,useRings}`、`mocks` 主体/候选 fixtures + `/api/subjects` handlers + `contract.test` 主体部分)自此**UI 零消费**(仅 `contract.test` 覆盖 + `rings` fixtures 依赖 `SUBJECTS` 派生成员)。作为后端契约层暂留,是否移除留作单独清理决定(不属本次「菜单去重」范围)。
- **门禁**:`typecheck` ✓ · `test` ✓ 61/61(未回归)· `build` ✓ · 触及文件 `eslint` clean。

## 二十三、团伙详情页补充 · 资金路径 + 关联告警(2026-07-01)
`RingDetail`(团伙详情 tab)此前有图谱/证据/置信度/成员,但资金流无独立视图、关联告警只有个数字。补两块:

- **资金路径**(图谱卡之后):分阶段资金流 `分散入金 → 归集 →〔中转分层〕→ 分发出金`。由 `fundStages(ring)` 按 洗钱手法(`typology` 含 分层/洗钱/过账/规避/归集 → 加中转层)+ 成员(拆分/入金/源/群组 计来源数)+ 金额(`amount` 解析,出金取 92%,≥1 万标「部分超 LCTR」)确定性合成;横向 stepper,各阶段按 `Tone` 着色(蓝→琥珀→紫→红),`ArrowRight` 连接。未套用案件页 `CaseFundGraph`(其依赖案件专用 `CaseGraph` 跨境分层结构,团伙数据无对应,成本高于自造)。
- **关联告警**(页尾):按团伙商户成员名匹配 `lib/data` 的**真实告警**(`nameMatch` 前缀/首词兼容 "NovaPay Technologies"↔"NovaPay Technologies Ltd.")。表格列 告警ID / 商户成员 / 类型 / 严重度(`sevMeta` Pill)/ 金额,行跳 `/alert?id=`;无匹配时降级显示「关联 N 条 · 见告警研判」+「全部告警」入口。
- **门禁**:`typecheck` ✓ · `test` ✓ 61/61(未回归)· `build` ✓ · 新增代码 `eslint` clean(`RingDetail` deep-link `useEffect` 的既有 react-hooks 报错未动)。

## 二十四、关联告警 · 与「告警研判」队列口径对齐(2026-07-01)
核对 §23 关联告警与现有告警队列(`/alerts` · AlertList)的一致性,发现两处口径差:
- **数据源一致**(都用 `lib/data` 的 `alerts`)、**ID 能解析**(AlertDetail `alerts.find(id)`),但——
- **状态车道未对齐**:队列只显示 `INVESTIGATION_STATES=[progress,pending_l2,escalated]`;`new` 归事中监控、`closed_*` 归处置记录。原关联告警按商户名**全状态**匹配,会列出队列里看不到的告警。
- **状态非实时**:原读静态 `a.state`,未走 `alertStore.stateOf`(队列用的会话内实时状态)。
- **修法**(不砍列表——团伙调查需看跨车道全部关联告警,而是标清):`LinkedAlerts` 改用 `useAlertVersion()` + `alertStore.stateOf` 取实时状态;新增「状态 · 车道」列(`RC_STATES` Pill,口径同队列)+ 车道标注(告警研判 / 事中监控 / 已处置);按车道排序(研判队列内的排前)+ 分数次序;顶部点明「共 N 条 · 其中 M 条在告警研判队列」。无匹配时文案改为「未匹配到当前告警库中的成员告警」。
- **门禁**:`typecheck` ✓ · `test` ✓ 61/61 · `build` ✓ · 新增代码 `eslint` clean(既有 `toggleCluster`/deep-link `useEffect` 的 react-hooks/no-unused-expressions 历史欠账未动)。

## 二十五、共享证据明细 · 补「共享要素」具体值(2026-07-01)
`RingDetail` 共享证据明细此前每条连线只给 主体对 + 强度 + 维度标签 + 一句 note,**看不到具体共享的是哪个要素**。补一行「共享要素」,把每个维度落到确定性合成的具体值:
- **提现白名单地址**:按团伙取**同一个** `0x…` / `bc1…`(体现「多商户共用同一白名单地址」;note 内已有地址 token 则优先提取,避免与文案矛盾)
- **设备指纹**:优先从 note 提取 `DV-N` 群,否则合成
- **IP 段**:按团伙取同一 `x.x.x.0/24`
- **资金**:按边合成跳数(`N 跳归集`)
- 合成 seed = `ring.id`〔+ 边序〕,`h32`/`fragHex`,刷新稳定;仅 UI 派生,不改数据。
- **门禁**:`typecheck` ✓ · `test` ✓ 61/61 · `build` ✓ · 新增代码 `eslint` clean(既有 `toggleCluster`/deep-link `useEffect` 历史欠账未动)。

## 二十六、团伙「批量列名单」处置做成闭环(2026-07-01)
此前团伙研判处置里「批量列名单」只改状态 + toast,不真写名单(名单里团伙来源条目是预置静态)。仿转案件 `intakeCase` 的收口模式补真闭环:
- 新增 `lib/listIntake.ts` · `intakeList()`:按处置抽屉所填 **名单类型 / 列入对象(多选)/ 有效期** 把团伙成员真实写入 `listStore`——
  - 名单类型 → `cat`(含「黑名单」→ block,余 → watch);有效期 → `expiry`(永久 → 长期有效)。
  - 列入对象 → `pickTargets` 命中成员(商户主体 / 关联群组 / 全部成员地址 · 对手地址簇);`entryType` 按成员 kind 映射(商户 / 个人 / 链上地址)。
  - 每条 `source="团伙" · srcId=ring.id · to=/ring?id=`(回链本团伙)、`addedBy=当前操作员`、理由取研判备注(空则自动生成)。id `LE-2026-2xxx`,世界日期 `2026-07-01`(与 `mkCase` 口径一致)。
- `RingDetail.submit()` 加 `disp === "watch"` 分支:调 `intakeList` → toast「已将 N 个对象列入{类型} · 已写入名单管理」+ 团伙状态转「已列名单」+ 活动日志记「批量列名单 · N 个对象」。
- 收口效果:`ListsPage` 已 `[...listStore.created(), ...LISTS]` + `useListVersion` 订阅,新条目即时置顶显示并可点回团伙。至此团伙两大终态处置(转案件 / 列名单)均为真闭环。
- **门禁**:`typecheck` ✓ · `test` ✓ 61/61 · `build` ✓ · 新增代码 `eslint` clean。

## 二十七、技术债清零 · 全站 eslint 归零 + 删 shadcn 死代码(2026-07-01)
前几节反复标注的「历史欠账未动」(`RingDetail` deep-link `useEffect` / `toggleCluster`)+ 全站散落的 lint 报警一次清干净,让门禁从「触及文件 clean」升级为**整仓 `eslint src` = 0 problems**。均为真修复,不是加 `eslint-disable` 掩盖(反而删掉了原有无效 disable)。
- **`react-hooks/set-state-in-effect`(渲染期 setState)** → 换 React 官方认可的**「输入变化时于渲染期调整 state」**模式(非 effect):
  - `RingDetail`:`?action=` deep-link 开处置抽屉,改 `if (action !== lastAction) { setLastAction(action); … }`,保留「action 参数变化即重开」行为;顺带删 `useEffect` import。
  - `AddToListDrawer`:抽屉 `open` false→true 时重置表单,改 `wasOpen` 跳变判定;删 `useEffect` import。
- **`@typescript-eslint/no-unused-expressions`(三元当语句)** → `RingDetail.toggleCluster` / `ReportDrawer.toggle` / `CaseDetail` 因子折叠 的 `has? del : add` 改 `if/else`。
- **`react-hooks/immutability`(渲染期改外部 `let`)** → `CaseDetail` 瀑布累计 `let cum` + `map` 改纯 `reduce`(累计值取上一项 `end`),无渲染期外部变量。
- **`@typescript-eslint/no-unused-vars`** → 删 `reports.ts` 未用 `JL`/`SC`、`Dashboard` 未用 `type Role`、`AddToListDrawer` 未用 `LENTRY_TYPE_TONE`;`eslint.config.js` 加规则项 `{ argsIgnorePattern:'^_', varsIgnorePattern:'^_', ignoreRestSiblings:true }`(`_` 前缀=有意忽略、`{ _pass, ...r }` 解构丢弃字段不算未用),回归测试 `_pass` 随之 clean。
- **`react-refresh/only-export-components`(组件文件混出非组件)** → 把 `bits.tsx` 的 `TONE` 色表 + `toneVar` **上移到 `lib/data.ts`**(与 `Tone` 类型同源,组织更合理),`bits.tsx` 回归纯组件文件;5 处消费方(`FindingReviewDialog`/`RingDetail`/`ReportDetail`/`StrategyPage`/`EntityProfile`)改从 `@/lib/data` 取 `toneVar`。
- **删 shadcn 死代码**:项目全程用 HeroUI,`src/components/ui/` 下 shadcn 脚手架**除 `sonner.tsx`(App 的 Toaster + 全站 `toast` 在用)外 17 个文件零引用**(`dialog→button` 为死链内部依赖),整批删除 —— 一并消掉 `badge`/`button`/`tabs` 的 `react-refresh` 报错与噪音。
- **`ThemeToggle` 去冗余 mounted 守卫**:SPA 无 SSR,首帧 `resolvedTheme` 为 undefined 本就默认 Moon、解析后自动纠正,`mounted` state + mount effect 纯属多余,删除后行为不变、少一个 effect。
- **门禁**:`eslint src` ✓ **0 problems(整仓)** · `typecheck` ✓ · `test` ✓ 61/61(未回归)· `build` ✓。

## 二十八、优化 backlog 复核 · 补 B1 需关注度排序 + B2 SLA 计算框架(2026-07-02)
拿到一份早期写的 `OPTIMIZATION_BACKLOG.md`(9 项 A/B/C)。逐项对照当前代码(并行审计)后发现**7 项早已实现**——A1 处置(TraceDrawer/FindingReviewDialog/案件止损)、A2 告警研判+案件管理(`/alerts`+`/cases` 全套)、A3 LVCTR、B3 置信度/60% 阈值(代码+`RingBasis` UI 已披露)、C1 力导向(≥6 人分层布局,12 主体样本不崩)、C2 我的工作台(仪表盘「待我处理」)、C3 误报闭环(`FREASONS`+`fp30`+规则回填)。**真缺的只有 B1、B2 两项,且都是 backlog 自己标注「前端不能拍板」的待定口径**。按 backlog 建议「前端只实现算法框架、现值占位标注待定」补上:
- **B2 · SLA 计算框架** —— 新增 `lib/sla.ts`:`SLA_HOURS`(high 24h/medium 48h/low 96h · 占位)+ `slaTier(confidence)` 分档 + `computeSla(elapsedH, tier)`(余量 / pct / tone / 逾期)。演示态无真实入队时间戳,`synthElapsedH(id, tier)` 由 id 稳定合成「已过时长」(0..1.3×时限,约 1/4 逾期);接后端改读真实 `detectedAt`。`rings.ts` 新增 `slaOfRing(ring,state)`(只对 待认领/调查中 车道有时限)。**删掉团伙 fixtures 里 5 处手写死的 `sla` 字段 + `Ring.sla` 类型**(改为计算派生,不留死数据);`RingList`/`RingDetail`/`Dashboard` 待我处理 三处 SLA 显示改用 `slaOfRing`。告警/案件/事后命中仍各自携带静态 SLA,待口径确认后统一迁移(已在 `OPEN_QUESTIONS.md` 注明)。
- **B1 · 需关注度排序** —— `rings.ts` 新增 `ATTENTION_WEIGHTS` + `attentionScore(ring,state,sla)`:需关注度 = 风险(置信度档)+ 待办(状态 bucket)+ 趋势(SLA 逾期/临期)+ 关联告警量(封顶)。`RingList` 行改为按需关注度降序,过滤区加「按需关注度排序 · 权重待定」标注(hover 说明公式)。权重全为占位默认。
- **`docs/OPEN_QUESTIONS.md`(新建)** —— backlog 引用但仓库缺失,补上。汇入 B1(权重表)/ B2(起算点+分级时长+逾期动作)/ B3(维度权重+60% 阈值背书)三项待合规确认的参数,每项标代码位置 + 当前占位值 + 待确认点。同步把 backlog 文件 9 项状态更新为已完成/已解决(附依据)。
- **门禁**:`eslint src` ✓ 0 problems · `typecheck` ✓ · `test` ✓ 61/61(未回归)· `build` ✓ · SLA 计算值抽样合理(剩 5h/15h/24h/36h/46h)。

## 二十九、前端一致性 + 可访问性扫盘 · 消除全站「对不齐」(2026-07-02)
视觉配色一致性已足,这轮补另一维度:**代码 DRY + a11y**,重点是「同一模式在所有页面统一到底,不留半拉子」。
- **tone→颜色助手收敛(消除 ~20 处重复)**:全站散落着近 20 份本地 `tc`/`toneCol`/`toneC`/`toneCss` 及内联三元,且**fallback 各不相同**(--text-2 / --text-3 / --success / --brand)——这正是最隐蔽的「对不齐」。新增 `lib/data.ts` 两个共享助手:
  - `toneColor(t)` —— 完整半映射(每 tone 落自己的色,grey/未知→--text-3),取代 6 份**逐字节相同**的 `tc`(CaseDetail/PostDetail/RuleDetail/ListDetail/CaseReviewDrawer/CaseFundGraph,改为 `const tc = toneColor` 别名,零调用点改动)。
  - `urgencyColor(t, fallback)` —— 只 red/amber 上色,正常态落 `fallback`。**fallback 显式传参**保留各处不同的静默色语义(SLA→text-3、置信度→text-2、运营健康→success、事中→brand),既消重复又零视觉变化。SLA 列(RingList/PostMonitoring/CaseList/ReportFiling)、Dashboard 4 处、OpsOverview/AnalystQueueDrawer/Monitoring/NewRingDrawer/RingDetail 全部改用它。
  - ReportDetail 报送时限 banner 的 bd/bg/fg 三元改用 `TONE[tone]` 三元组(与 `Pill` 同源)。
  - 校验:全站残留 `=== "red" ? "var(--danger)"` 内联三元 = **0**。
- **可点击行:对齐 + 键盘可达**:`RingList` 原来整行 `onClick` 导航(鼠标可点、**键盘/读屏不可达**),而其它列表页都用行内按钮——它是唯一异类。改为**名称单元格包 `<button>`**(可聚焦、可回车)+ 保留 Eye 按钮,去掉整行 `onClick` 与 `cursor-pointer`,与 Monitoring 的「名称按钮 + Eye」模式统一。`Monitoring` 的 `tr` 上还有一处**误导性 `cursor-pointer`**(行其实不可点)一并删除。校验:整行 `onClick` = 0、误导 `cursor-pointer` = 0。
- **图标按钮 aria-label 全覆盖**:17 个 `isIconOnly` 按钮原仅 3 个有无障碍名(其余靠 Tooltip,AlertList 的 3 个连 Tooltip 都没有)。给全部补 `aria-label`(复用 Tooltip 文案)——**17/17 覆盖**。
- **门禁**:`eslint src` ✓ 0 problems · `typecheck` ✓ · `test` ✓ 61/61(未回归)· `build` ✓。均为零视觉回归的等价改写。

## 三十、新模块 · 链上风险情报(KYT)原型页(2026-07-02)
补业务盘点里"纯风控口径下最缺、且加密专属"的一块:**链上风险情报层**。核心是把职责分层演示清楚——**情报=买供应商,策略与处置=本系统自建**(这才是风控的活)。
- **`lib/onchain.ts`(数据 + 策略)**:
  - **供应商侧(mock)**:`AddrRisk`(地址 / 链 / 0-100 风险分 / 类别归属 `RiskCat` / 资金敞口 `Exposure`〔占比+跳数+流入流出〕/ 对手方 VASP / 见于本系统交叉链接)。9 条确定性样本,覆盖 被制裁直接敞口 / 混币器 / 暗网 / 诈骗 / 盗币 / 干净交易所 / DeFi / P2P。真集成时接 Chainalysis/TRM/Elliptic(见 FULLSTACK_MVP §6,fail-closed)。
  - **本系统侧(自建)**:`KYT_POLICY`(阈值=风险偏好,风控总管可调·占位待定)+ `policyVerdict()` 把供应商信号映射成处置 **拒绝 / 转人工 / 放行** 并给命中原因(直接制裁敞口→硬拒;分≥90→拒、≥50→复核;混币器≥15% / 暗网≥5% / 诈骗·盗币≥10%→复核)。`screen()` 支持筛查任意地址(命中样本或按地址串确定性合成)。
- **`pages/OnChainIntel.tsx`(`/onchain`)**:情报源/职责边界 banner(制裁 STR 报送归合规系统)+ 4 汇总(已筛查 / 拒绝 / 转人工 / 命中制裁)+ 地址粘贴筛查 + 表格(地址 / 链 / 供应商风险分〔severity 色〕/ 类别〔中性灰〕/ **本系统处置**〔Verdict 色〕/ 命中策略 / 见于 / 查看)。
- **`components/AddrRiskDrawer.tsx`**:地址风险画像抽屉——处置判定 banner(强调"供应商给信号→本系统据策略处置")+ 风险分条 + 类别归属(灰)+ **资金敞口条**(按类别/跳数/流向,severe 上色)+ 对手方 + **见于本系统**(回链 商户`/entity` / 团伙`/ring` / 告警)+ **生效风险策略卡**(本系统自建·可调·待定)+ 列入观察名单 / 去监控规则。
- **接线**:`App.tsx` lazy `/onchain`;`Shell` 检测策略组「团伙识别」下加「链上情报」(`Link2`)。
- **配色守「只有状态用色」**:风险分/级(severity 焦点)+ 处置判定(工作流决策)+ 敞口条(图表)用色;类别归属 / 对手方 / 链 中性灰。
- **门禁**:`eslint src` ✓ 0 · `typecheck` ✓ · `test` ✓ 61/61 · `build` ✓。

## 三十一、链上情报三连 · 接入事中闸口 + 团伙成员风险分 + 策略可调(2026-07-02)
把 §30 的 KYT 情报从"独立页"打通进全站三处:
- **① 接入事中闸口决策引擎(适配器,不改引擎)**:决策引擎 `lib/decision.ts`(44 测试)本就有 `kyw`/`mixerHops`/`addressTags` 字段。新增 `onchain.kytToSignals(addr)` 把地址画像转成这些链上信号(`kyw←风险分`;`mixerHops←制裁/混币最短跳`;`addressTags←自身类别 ∪ 敞口所及高危类别`——让"敞口到暗网"也进信号)。在 `AddrRiskDrawer` 加「事中闸口影响(示意)」:以该地址为收款方一笔 CAD 2,500 提现,**真跑 `decide()`** 展示引擎判定(冻结/拦截/暂缓/转研判/放行)+ 依据。**这也给了 `decide()` 首个 UI**。抽样:混币器地址→冻结、干净交易所→放行、暗网敞口→转研判、45 分 P2P→放行(体现"供应商给分≠引擎处置")。
- **② 团伙成员挂链上风险分**:`RingDetail` 成员卡里 `kind==="地址"` 的成员用 `screen(name)` 取确定性风险画像,名旁加「链上风险 N」Pill(按等级上色),点名可开 `AddrRiskDrawer` 看完整画像 —— 团伙关系图 × 链上情报打通。
- **③ 风险策略可调面板**:新增 `kytPolicyStore`(内存 · `useKytVersion` 订阅)+ `policyVerdict(a, policy)` 改吃实时策略。链上情报页加「风险策略」折叠卡:7 个阈值(分数拒绝/复核、制裁跳数、混币/暗网/诈骗/盗币敞口),**风控总管可调**(`roleStore` head 门控,分析师只读锁定),改动即时重算全表判定 + 抽屉 + 团伙成员分;非默认值标「已调整」+「恢复默认」。
- **配色守「只有状态用色」**:风险分/等级、处置判定、引擎动作用色;类别/对手方/阈值输入中性。
- **门禁**:`eslint src` ✓ 0 · `typecheck` ✓ · `test` ✓ 61/61(决策引擎未回归)· `build` ✓。

## 三十二、链上情报做成共享卡 `OnChainRiskCard`(2026-07-02)
按"供应商情报放详情页展示"的思路(用户设计稿)把链上情报抽成一个**可复用卡组件**,并按 4 点打磨:
- **新增 `components/OnChainRiskCard.tsx`**(详情页 / 抽屉 / 控制台共用,避免各画一套 → 防"对不齐"):环形风险分仪表 + **本系统处置**(放行/复核/拒绝,买情报之外"建"的那半就地展示)+ 暴露分析条 + 命中要点 + 扫描历史 + 情报源标注。
- **① 暴露条按风险着色**(而非全蓝深浅):高危类别(混币器/暗网/制裁…)红、中(博彩)琥珀、低(交易所/DeFi/P2P)中性 —— `catRiskTone`/`catRank`,severe 排前,一眼看出"红的占多少"。
- **② 扫描历史带分数 + 趋势**:`scanHistory()` 确定性合成历次扫描(分数向当前收敛、日期回溯),每条显 日期 + 当时分数 + 涨跌箭头(升红/降绿),取代原设计"一排相同时间戳、无值"。
- **③ 补"本系统处置"**:卡内直接显 `policyVerdict` 结论 + 依据(供应商给信号、本系统按策略给动作)。
- **④ 标情报源 + 扫描对象**:头部"扫描对象 0x…"+ "情报源:KYT 供应商" + 下载报告。
- **`riskFindings()`**:由敞口数据派生命中要点(severe 敞口→风险条;无制裁→"未检测到直接制裁匹配 · OFAC/UN/EU/FINTRAC"绿色确认)。
- **`AddrRiskDrawer` 重构**:改为渲染 `OnChainRiskCard` + 抽屉专属(事中闸口影响 / 对手方 / 见于本系统 / 策略卡 / 底部动作),去掉原来手搓的分数/暴露/判定(去重)。`/onchain` 表格与团伙成员抽屉自动用上新卡。
- **产品判断记录**:链上情报"能力"必做(入金来源=对手方地址是核心风险,且不在白名单上);形态优先级 = 详情页嵌卡 + 接事中闸口 > 独立控制台页(后者降级为"临时筛查 + 高风险/白名单复审队列")。`AlertDetail` 已有按告警手写的对手地址情报 + Chainalysis 溯源卡,暂不塞合成数据卡以免与其叙事冲突;共享卡可随时按真实情报数据嵌入。
- **门禁**:`eslint src` ✓ 0 · `typecheck` ✓ · `test` ✓ 61/61 · `build` ✓。
